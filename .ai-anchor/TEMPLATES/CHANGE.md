# 变更卡：<一句话意图>

> 复制本文件到 `changes/YYYY-MM-DD-<slug>/CHANGE.md` 后填写。
> 一页为限。写不完说明这件事该拆成两次变更，而不是写长一点。

- **变更编号**：（与所在目录名一致）
- **日期**：
- **车道**：快车道 / 标准道 / 改道 ——（判定依据：本次动的东西在不变量清单里吗？）

## 意图

这次要达成什么。**一句话，且必须能被验证**。

- 反例：「优化一下首页体验」——无法判定做没做到。
- 正例：「让首页书架显示的篇数与实际文件数一致」——能跑一遍数出来。

## 范围

本次要动的文件、目录、章节，逐个列出（用仓库内相对路径）。

-

## 非目标

**本次明确不做的事，至少两条。** 这一行性价比最高：它挡住 AI 最典型的一种失误——
顺手帮你多做了几件你没要的事。

- 不做：
- 不做：

## 验收标准

怎么算做完了。**每一条都要可判定真假，能跑出结果的优先写成命令。**

| # | 验收项 | 怎么验（命令 / 动作） | 通过标准 |
| --- | --- | --- | --- |
| 1 | | | |
| 2 | | | |

> 写不出来的，最后一定演变成"我觉得还行"。写不出就先退回立锚，不要往下走。

## 影响面

逐条对照 `PROJECT.md` 第五节的「不变量清单」：

| 不变量 # | 本次是否触碰 | 说明 |
| --- | --- | --- |
| 1 | 否 | |
| …… | | |

结论：**不触碰任何不变量** → 可走快车道或标准道；
**触碰第 N 条** → 走改道：先单独开一轮改 `PROJECT.md`，再回来做本次变更。

## 风险与回滚

- 最坏会怎样：
- 怎么退回去：（`git revert` / `git checkout -- <文件>` / 还原上一版锚点）
- 回滚会不会丢内容：

## 待裁决

**需要人来拍板的选择题。给选项，也要给每个选项的代价。**

| # | 选项 | 代价 | 我的倾向 |
| --- | --- | --- | --- |
| 1 | A： / B： | | |

> 这一栏只有人能填。AI 可以起草，但必须由人确认后才算成立。

## 收尾

完成后要更新 `PROJECT.md` 的哪几节，以及是否需要在 `DECISIONS.md` 追加一行。

-

## 过闸检查（进入第②步前必须全部为是）

```text
[ ] 验收标准可以被判定真假
[ ] 非目标不是空的（至少两条）
[ ] 影响面已逐条对照不变量清单
[ ] 待裁决项给了选项与代价
[ ] 车道已判定并写明
```
