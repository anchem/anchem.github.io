# 评审单：`2026-09-22-playbook-split-rebuild`

- **评审角色**：独立评审者（未参与本次变更，未读过创作过程对话）
- **评审时点**：2026-09-22
- **评审依据**：`PROJECT.md`、本变更 `CHANGE.md` / `DIAGNOSIS.md`、`docs/selfdevelop/cognition/thinking/critical-thinking-playbook/` 下 13 篇 md + `_category_.json`、`git show HEAD:docs/selfdevelop/cognition/thinking/critical-thinking-playbook.md`（原文 800 行 / 33951 字符）、`docs/selfdevelop/cognition/thinking/{index,criticalthinking}.md` 的 git diff、`build/sitemap.xml`、`build/docs/**` 产物
- **独立复算脚本**（评审依据，可复跑）：`.workbuddy/review-tmp/{metrics,explore5,final_metrics,density,anchors,links,compliance}.py`
- **纪律说明**：本单只挑错，不提供修改方案；`docs/` 下文件只读，未作任何改动。

---

## 结论

**不通过。** 4 条阻断项，其中 2 条是内容/约定级（引号字符集被整体换掉、一处加粗与引号相邻的写法被改回不合规形式），2 条是验收证据级（验收 5/6/7 的数字复算不出、验收 10 的“ok”是脚本恒真输出）。

事实层面的核查结论（陈述，非褒扬）：原文的五张清单条目、22 个偏误名、14 种谬误、18 条动作（含五字段与全部数字门槛）、30 天逐日任务、八个误用、28 行书目地图、3 处同源标注、3 处分歧、附录速查卡，逐项都能在新版里找到，且**没有新增任何原文没有的书目**；唯一确凿的内容缺失是 `review.md` 季度热力图表少了「趋势」一列（B3）。

---

## 阻断项（必须改）

### B1　13 篇把原文的引号字符集整体换掉了，与 `PROJECT.md` §7 明文不符

- **位置**：新目录 13 篇全部。典型：`index.md:12`“这份手册只做一件事：**把「判断」从一个态度…**”、`checks.md:23`“只把它标记成「待核」”、`foundations.md:14`“**偏误、谬误、噪声经常被混着说…**”（同段内的「」共 6 处）。
- **实测**：原文 `“` 213 处 / `「` 3 处；新目录 `“` 15 处 / `「` 265 处。逐篇：actions 31、checks 33、background 27、foundations 24、judgment-flow 23、review 21、decide 20、creativity-ai 18、appendix 17、guards 17、pitfalls 16、plan 12、index 6（计 `「`）。
- **理由**：`PROJECT.md:165`（§7「引号」）写的是“中文句子（含表格、提示框）里的引号一律全角 `` “ ” ‘ ’ ``”，未列 `「」`；同日下午落地的 `.workbuddy/fix-playbook-quotes.js` 头部注释写明它就是在执行“站点口径”，把 `**“一半的真理”**` 改成 `“**一半的真理**”`；对照成稿 `docs/selfdevelop/outlook/humanity/human-abilities-in-ai-era.md` 是 `“` 137 / `「` 6。CHANGE.md 的「范围」与「非目标」都没有申报这项改动。
- **需要并存认定的事实**：站内另有 165 个 docs 文件使用 `「`（共 1529 处），说明它不是孤例。因此本条同时构成「待裁决 Q1」；若裁定 `「」` 可用，须按 §5「改道」先改 `PROJECT.md` §7 再回填本次结果。

### B2　既有的合规写法被改回不合规：加粗包住引号

- **位置**：`docs/selfdevelop/cognition/thinking/critical-thinking-playbook/checks.md:133`：`绝大多数谬误不是明目张胆的胡说，而是**「一半的真理」**。`
- **理由**：§7（`PROJECT.md:166`）写“加粗与引号相邻时，加粗加在引号内（写 `` “**重点**” ``，不写 `` **“重点”** ``）”。原文此处是`“**一半的真理**”`（合规），上一轮的 `.workbuddy/fix-playbook-quotes.js` 里还专门有一条 `['而是**“一半的真理”**——', '而是“**一半的真理**”——']` 修过它，本次把它写成了 `**「一半的真理」**`——引号字符集与嵌套方向同时出错。
- **同类、待同一裁决**：`background.md:12`：`**「把东西做出来」不再是瓶颈。**`（加粗跨到引号外）。

### B3　复盘表少了一个字段

- **位置**：`review.md:114-120` 季度偏误热力图表。
- **实测**：原文 §6.4 该表 5 列（`信号类别（归并自 6.2 的八条） | 第 1 月 | 第 2 月 | 第 3 月 | 趋势`），新版 4 列，最后一列「趋势」消失；而正文 `review.md:110` 与 `:112` 仍在要求“看趋势，不看绝对值”。
- **理由**：CHANGE.md:34（非目标）写明“不改任何一张表的判据”；CHANGE.md:50（验收 9）写明“四张复盘表…数量与改前一致”并判为“通过”。该表是给读者填写的模板，删掉一列等于拿走一个填写位，验收 9 的结论不成立。

### B4　验收 5/6/7 的数字复算不出，验收 10 的证据是脚本恒真输出

- **位置**：`CHANGE.md:46`（验收 5）、`:47`（验收 6）、`:48`（验收 7）、`:51`（验收 10）。
- **我的独立复算**（口径：剔 front matter / 代码块 / 表格行 / 列表项 / admonition 块；`[文字](链接)` 折成文字；句子按 `。！？；` 切；字数为不计空白的字符数）：
  - **引用密度**：原文全书 `《》` 136 处 ÷ 整篇 33951 字符 = **4.05/千字**（与卡片“4.00”只差分母取法）。新稿按卡片自述的排除法（12 篇、不含 `appendix.md`、剔除各篇「本篇出处」小节）是 10 处 ÷ 35538 字符 = **0.28/千字**；若把 appendix 与各篇出处一并计入则为 3.44/千字。**两种算法都得不到 0.43。**
  - **句子**：原文超 60 字 **17** 句（与卡片一致）；但“最长句 148”复现不出——同口径下原文最长句为 **104** 字（不折链接、列表项独立成段时为 173 字），“超 80 字 7 句”同口径下为 **5** 句；新稿最长句为 **64** 字（卡片写 70）。
  - **段落**：原文中位 **78**（一致）、最长 **211**（卡片写 201）；新稿中位 **48**（一致）、最长 **149**（卡片写 147）。
  - **验收 10 的“13 个 ok”**：`.workbuddy/check-style-one.js` 第 47 行是无条件 `console.log('ok')`，与检查结果无关，因此“ok”不构成通过信号；同一次运行中它把 13 篇每篇都标出“非行首加粗 3～16 行”（这是 §7 允许的句中加粗；§7 已写明该脚本所依据的 `bold-normalize.js` 规则“不可用来校验 docs 长文”）。
- **理由**：验收表是第⑤步归集的判据。数字与口径必须二选一改掉（补可复现的脚本，或改数字），否则验收 5/6/7 无法判定真假；验收 10 需换成有断言力的证据。

---

## 建议项（可以不改）

- **S1　篇末出处与正文不对应（7 处）**，与 DIAGNOSIS 5.2 ⑤“本页所有《书名》集中在这里，按小节列，一段一段对得上”不符：
  - `decide.md:149`「框架效应：换个说法就换个选择」→《思考，快与慢》：正文第 2 问（`:41-45`）已不再出现“框架效应”这个词。
  - `guards.md:69`「观点形成的层次」→《超越感觉：批判性思考指南》：该主张在 `review.md:25`，不在本页。
  - `checks.md:155`「理性与情绪的关系」→《独立思考》：该主张在 `background.md:73`，不在本页。
  - `checks.md:154`「识别隐藏假设与省略信息」→《学会提问》：本页正文只承接了“分清论题与结论”；“省略信息”在 `judgment-flow.md:129`。
  - `foundations.md:122`「思维八要素与判断标准」→《批判性思维》（保罗 & 埃尔德）：本页正文无此主张（“思维八要素”只在 `appendix.md:60` 书目地图里）。
  - `review.md:126`「观点的几个层次、抗拒与思想狭隘」→《超越感觉》：只有“观点的两个档位”在本页，`抗拒与思想狭隘` 在 `pitfalls.md:74`。
  - `review.md:128`「后见之明偏误」→《决策与判断》：本页正文只说“结果污染”，未提该名词；这条归属是从 `actions.md:158` 动作 1 的依据转来的。
  - `review.md:129`「输出与领域反馈」→《创造力：心流与创新心理学》：这句是 `actions.md:174` 动作 17 的语境。
  - `creativity-ai.md:51`「AI 抬高下限，人负责上限」→《拐点》：本页正文讲的是“从一到无穷 / 从零到一”，“抬高下限/上限”在 `background.md:72`。
- **S2　`review.md:39`** 给季度复盘定了“20 分钟”，原文该周期没有用时（原文 §6 只给每周 10 分钟、每月 30 分钟）。数字 token 在原文别处存在，但这一处搭配是新的。
- **S3　`checks.md:12`**“一天里从你眼前过去的信息，九成不值得动用判断力”是新增的量化判断，原文无此说法；`PROJECT.md:105`（§6.1 门禁「素材核查」）要求“所有硬数据都有出处；无法核实的数字宁可留白”。
- **S4　`review.md:74-79`** 判断准确率表把原文的「#」列与「事后看，当时漏了什么」列并成了表后一行文字。字段未丢，但作为可打印填写的模板，建议恢复成列。
- **S5　“正文只留主张”没有做到零**：正文仍留 10 处《书名》（`decide.md:24-27` 四处、`index.md:14` 四处、`checks.md:33`/`:39` 两处）。按 DIAGNOSIS R1“句子里的引用压到零”不合格，按验收 5“≤1 处/千字”合格——见「分歧点 P3」。
- **S6　`index.md:16`**“十二篇各写一件事”与 `CHANGE.md:18`“新目录下 13 个文件”口径不一（该目录实际是 13 篇 md，其中 index 为导览页）。表述统一即可，不影响内容。
- **S7**　各篇新增块的体例不一（`background.md:60`「三条自查」、各篇「带走的清单」、`decide.md:135`「带走的清单」），与 DIAGNOSIS 5.2 的“八件套”并不一致——这属裁决 3 明确放开的范围，仅作记录。

---

## 偏离项（对照变更卡的「范围」「非目标」与 §9）

- **D1**　`review.md:114-120` 删掉「趋势」列 —— 与「非目标」第 3 条（不改任何一张表的判据）冲突（同 B3）。
- **D2**　**书目口径有未申报的改动**（「非目标」第 3 条写明不改书目口径）：
  - 《超越感觉》（原文 5 处）→ 新版一律写全《超越感觉：批判性思考指南》；
  - 《思维突破》（原文 3 处）→ 新版《思维突破：如何获得好想法》（`appendix.md:77`）、`creativity-ai.md:49`，`appendix` 之外只剩 1 处；
  - 《创造性》（原文 2 处：`3.5` 与动作 17 依据）→ 新版挂到《创造力：心流与创新心理学》名下（`actions.md:174`、`review.md:129`）。按原文 3.7 的说明，`《创造性》`指的应是《创造性：发现和发明的心理学》那一译本，新版换成了另一译本的名字；
  - 《责任圈思维模型》原文写成 `《书名》`（3.4 九步第 1 步），新版改成站内链接（`judgment-flow.md:67`、`:134`）。
  以上都不是“新增了一个来源”，但没有一项在变更卡里申报。
- **D3**　**新增了原文没有的主张**：`checks.md:12`「九成」、`checks.md:87`「判断力是稀缺资源，不能平摊给每一条推送」、`background.md:60-68`「三条自查」、`background.md:26`「这件事不会以「能力下降」的样子出现，只会以「越来越顺手」的样子出现」、`review.md:39`「20 分钟」。DIAGNOSIS 四节末写明“文字的减少只来自四件事：长句拆短、重复合并、出处外移、宽表改小节”——只授权了“减少”的来源，没有授权新增主张。
- **D4**　**「收尾」未完成**（截至评审时点）：`PROJECT.md:248`（§10 第 8 条）仍写着“`critical-thinking-playbook.md` 单篇 **800 行**…下一轮应优先处理：拆章时一并把 §7 由五列表改成八个小节”，而该文件已在本次被删除——这条已知风险现在描述的是一个不存在的文件；§7 里也没有新增“长文何时该拆章”一行；`DECISIONS.md` 没有本次的一行（该文件里最近一条是 `playbook-readability-edit`）。
- **D5**　**§9 的三项请示在仓库里没有书面记录**：新增目录、批量重构（>5 文件）、删除已发布文件。`CHANGE.md:9-11` 写明三项已获授权，但我在仓库里找不到获批凭证（见「未能验证 T8」）。**已合规的部分**：没有改 `sidebars.js` / `docusaurus.config.js` / 任何 `src/`；旧产物按 §6.2 与 ADR-004 做 `Rename-Item` 而非删除。
- **D6**　**`CHANGE.md:42`（验收 1）写“通过（15 个文件）”**：同一行自己给的构成是 `_category_.json` + `index.md` + 12 个章节文件 = 14；`ls` 实测 14 项。数字写错。

---

## 不变量核验（`PROJECT.md` §5，12 条逐条）

| # | 判定 | 依据 |
| --- | --- | --- |
| 1 | 已核验（未触碰） | 四个板块目录名未动；新增的是 `docs/selfdevelop/cognition/thinking/` 下的子目录 |
| 2 | 已核验（未触碰） | 站点名、站点 URL、`CNAME` 均未出现在本次 diff 中 |
| 3 | 已核验（满足） | 新目录有 `_category_.json`；`index.md` 2.3KB，含“划边界 / 五张清单 / 总纲 / 怎么读”四节、2 张表、12 条篇目链接，非空壳 |
| 4 | 已核验（满足） | 排序只用 `sidebar_position`（1–13，无重复）；目录名 `critical-thinking-playbook`、label「批判性思维实战手册」、文件名均无数字前缀 |
| 5 | 已核验（满足） | 13 篇 front matter 键位一致：`sidebar_position` / `title` / `description` / `tags` / `keywords`（脚本核，异常 0） |
| 6 | 已核验（不涉及） | 未改 `blog/` |
| 7 | 已核验（不涉及） | 未改 `blog/` |
| 8 | 已核验（不涉及） | 未改 `src/css/custom.css` 与任何色令牌 |
| 9 | 已核验（不涉及） | 未改天空层 |
| 10 | 已核验（满足） | 未编辑 `build/`、`build_old*/`、`node_modules/`、`.docusaurus/`；旧产物被 Rename 成 `build_old_split` |
| 11 | 已核验（满足） | 内链结尾斜杠 0 处；`node .workbuddy/check-doc-links.js docs` 报“共检查 238 个文件，问题 0 处” |
| 12 | 已核验（满足） | 裸 `<` `>` 0 处；表格单元格内加粗 0 处；无未转义 `|`（全站该目录 0 处 `\|` 需求，各表列数一致）；4 个 admonition 块（`index` / `checks` / `pitfalls` / `appendix` 各 1）都在表格之外 |

未出现“被破坏”；也未出现“无法判断”（本条 12 项均可判定）。

---

## 分歧点

- **P1　6 个章节级锚点的退役理由，成立一半。**
  - *成立的一半*：“Docusaurus 不为 H1 生成锚点”已实证——`grep '<h1' build/docs/**` 得 351 个 `<h1>`，**全部无 `id`**（含 `build/docs/selfdevelop/cognition/thinking/critical-thinking-playbook.html` 与 `.../critical-thinking-playbook/actions.html`）；`@docusaurus/mdx-loader/lib/remark/headings/index.js` 确实会给所有标题写 `properties.id`，但文档首个 H1 被提为页面标题、不落在正文里，因此这 6 个 ID 没有可重挂的位置。
  - *不成立的一半*：“站外深链（如 `.../critical-thinking-playbook#actions`）仍然能打开**正确的那一页**”这句话不成立。这 6 个 ID 指向的小节内容已整块搬走：`#background`→`background.md`、`#actions`→`actions.md`、`#plan`→`plan.md`、`#review`→`review.md`、`#pitfalls`→`pitfalls.md`、`#core`→`foundations.md`；用户跟着旧深链落到的是导览页，那一页已不含他要找的内容。“只是不会再滚动到某个小节”这句话把“页面打不开”与“内容不在这一页了”混为一谈。
  - 另外，6 个退役 ID 里有 **5 个与新建子页面的 URL slug 同名**（background / actions / plan / review / pitfalls），旧深链的“正确新地址”其实很好写：`.../critical-thinking-playbook/<同名>`；卡片只举了 `#tool-info` 一例，没写这一层。
  - *站内复核（这一条成立）*：全量扫描 `docs/**/*.md` 与 `blog/**/*.md`，指向这 6 个 ID 的链接**为 0**；全文引用了它们的 9 处（原文 1.4 导览表 8 处 + 3.4 一处 `#plan`）都已改到对应的新文件或新锚点，没有留死锚点。
  - *卡片自身口径冲突*：`CHANGE.md:45`（验收 4）写“所有**小节级**锚点（原 H2 / H3 / H4）一个不丢”，而丢掉的 6 个正是 H2。
- **P2　R5 与验收 8 互相矛盾。** DIAGNOSIS 四节 R5（`DIAGNOSIS.md:197`）要求“不能拆的压到 3 列以内”，验收 8（`CHANGE.md:49`）只要求“≤4 列”。新版 `review.md:74`（4 列）与 `review.md:114`（4 列）**满足验收 8、不满足 R5**；且上一轮评审留过一条未结事项“§7 宽表版式，留给拆章时一并处理”（`DECISIONS.md:53`），本次只处理了「八个误用」。
- **P3　R1 与验收 5 互相矛盾。** R1（`DIAGNOSIS.md:193`）要求“句子里的引用压到零”，验收 5（`CHANGE.md:46`）只要“≤1 处/千字”。实际正文留 10 处《书名》（见 S5）。两条判据同时写在本次材料里，验收时按哪条，需要定。
- **P4　`_category_.json` 的 `collapsed` 取值。** §7（`PROJECT.md:160`）规定“第 2 层及以下 `true`”，新目录写了 `true`；但同级目录实际都是 `false`（`docs/selfdevelop/_category_.json`、`cognition/`、`thinking/`、`learning/` 四份实测均为 `collapsed: false`）。新目录合 §7、不合现状。
- **P5　“12 篇 / 13 篇”两种口径**（`CHANGE.md:18` 说 13 个文件、`CHANGE.md:105` 说“最终 12 篇”、`index.md:16` 说“十二篇”、`CHANGE.md:23` 范围里又写“`_category_.json` + 13 个 md”）。这一次的实际文件数是 13 篇 md（含导览页）+ 1 份 `_category_.json`。

---

## 待裁决

| # | 事项 | 需要哪一种裁决 |
| --- | --- | --- |
| Q1 | 本站引号口径到底是 `` “ ” `` 还是 `「」` | 若定 `「」`，走 §5「改道」先改 `PROJECT.md` §7；若定 `` “ ” ``，B1 覆盖的 265 处需回改 |
| Q2 | 6 个退役锚点的代价表述是否接受 | 是否把“内容已搬走、旧深链落到导览页会找不到”写进 `CHANGE.md:58-69` 的代价核算；是否补一句 5 个同名子页面的新地址 |
| Q3 | 验收 5/6/7 以哪一套口径为准 | 我可以提供本次使用的脚本与切分规则，供与产出方口径对齐后重算 |
| Q4 | 「不改任何一张表的判据」是否涵盖“删掉一个空列” | 决定 B3 是回填「趋势」列，还是改验收 9 的措辞 |
| Q5 | 收尾（D4）是并入本次，还是另开一轮 | `PROJECT.md` §10 第 8 条现在描述了一个已被删除的文件 |

---

## 未能验证的部分

- **T1　「78 个偏误名」这个数字找不到对应物。** 我按原文逐条清点，§3.2 的偏误名是 **22 个**：获取信息时 4（确认偏误、可得性偏差、选择性知觉、锚定效应）＋解释信息时 5（事后诸葛亮偏误、叙事偏误、归因偏误、代表性偏差、幸存者偏差）＋做决定时 8（损失厌恶、沉没成本、现状偏好、过度自信、计划谬误、框架效应、禀赋效应、承诺升级）＋受他人影响时 5（从众、权威偏误、互惠、稀缺、嫉妒）。新版 `foundations.md:37-84` 是同一批 22 个，只是把“做决定时”拆成了两组。原文的其他计数是：《书名》136 处（去重 34 种）、谬误 14 种、动作 18 条。“78”与上述任何一个都对不上，来源无法核对。
- **T2　验收 5/6/7 的实际口径与脚本。** 复算不出 0.43 / 148 / 70 / 201 / 147 / 7 这六个数字；`.workbuddy/` 下也没有本轮使用的统计脚本（只有上一轮的 `an-*.js`、`tmp-stats.js` 等），因此无法确认产出方实际怎么切句、怎么定分母。
- **T3　「构建 exit 0」。** 我没有重跑构建（§6.2 要求先把 `build/` 改成 `build_old`，§9 红线 1 禁止编辑 `build/` 下任何文件）。只能给旁证：`build/` 目录时间 `17:28:18` 晚于全部源文件的最新时间 `17:27:29`；`build/sitemap.xml` 含旧 URL `.../critical-thinking-playbook` 与 12 个子路径共 13 条；`build/docs/selfdevelop/cognition/thinking/critical-thinking-playbook.html` 与 12 个子页面文件均已产出。
- **T4　「首轮构建报 4 处断锚点，修后复跑 0 处」**（`CHANGE.md:53`）：没有可查的构建日志，无法验证。
- **T5　验收 2 的“结构体检脚本”**（`CHANGE.md:43`）：`.workbuddy/` 下只有 `struct-check.txt` 等报告，没有对应的 `.js`，无法复跑；我只做了人工核对（见不变量 3）。
- **T6　三处同源标注本身是否正确。** “《精准决策》＝《哈佛商学院判断与决策心理学课》”“契克森米哈赖两译本”“保罗 & 埃尔德同框架两本”这三条，我没有查证原书原题 / ISBN / 目录，只能确认新版与原文逐字一致（`appendix.md:22-24`、`:83`）。
- **T7　28 本书的书目事实。** 书名、作者、译者与真实出版物的一一对应未查证；尤其无法确认《一只摸了就中奖的鸭子》《思维的陷阱》《一读就上瘾的逻辑学》三本的实际书目信息，也无法确认“《责任圈思维模型》”在原文中被写作书名是否另有所指。
- **T8　§9 三项授权是否有书面凭证。** “新增目录 / 批量重构 5 个以上文件 / 删除已发布文件”都属「必须请示人」；`CHANGE.md:7-11` 记载已获批准，但仓库内没有对应的获批记录或 `DECISIONS.md` 留痕，我无法核实。
- **T9　“站内 48 处锚点链接已逐条改到新路径”（`CHANGE.md:69`、`:96`）这个总数。** 我数到原文带锚点链接 51 条、新版 44 条（口径可能不同）；能确认的是：指向这 6 个退役 ID 的 9 处引用全部改到了正确落点，新目录内 106 条链接、44 条带锚点全部命中（0 处失效）。“48”与 DIAGNOSIS 问题 7 里“跨文件 10 余处”这两个数字我无法核实。
- **T10　站外深链的实际失效面。** 无法统计（与 `CHANGE.md:69` 的自述一致）。
- **T11　与 `human-abilities-in-ai-era.md`、`fromknow2done.md` 的“既有引用口径”是否仍然一致**（`DIAGNOSIS.md:252` 把这条列为“不动的部分”）。我只做了链接与锚点级核对：`#pbl`、`#division`、`#ceiling` 均存在且命中；内容级的口径比对只抽查了“上限 / 天花板”一处，未逐句比对。
- **T12　新目录 13 篇的内部一致性瑕疵之外，是否有我未读出的语义变形。** 我逐条比对的是：五张清单条目、22 个偏误名、14 种谬误、18 条动作的全部字段与数字门槛、30 天逐日任务、四张复盘表、八个误用、28 行书目地图、3 处同源、3 处分歧、附录速查卡、延伸阅读 11 条。除此之外的散文级改写（如把“有人把这件著作概括得很准”替换掉具体出处后的语气变化），我只做了抽样阅读，未做全文逐句对照。
