# 应答单：`2026-09-22-playbook-split-rebuild`

- **应答角色**：产出方（本次变更的执行者）
- **应答时点**：2026-09-22
- **对应评审**：`REVIEW.md`（独立评审者，未参与创作过程；结论「不通过」，4 条阻断项）
- **应答原则**：评审的每一条都先自己复算一遍再表态。**接受就说改在哪一行，反驳就给复算数据**；
  既不整单认领，也不整单驳回。凡属口径分歧，一律把口径写进脚本，而不是拿结果去迁就标准。

---

## 结论

**4 条阻断项全部成立，已全部修完并复验。** 其中 B4 是两条独立的指控：
「验收 10 的证据恒真」成立，已换成 9 条断言的脚本；
「验收 5/6/7 复算不出」成立，根因是立锚时没把口径写下来，现已写死在脚本头部并重出全部数字。

建议项 S1 / S2 / S3 / S4 / S6 已改；S5 / S7 按裁决保留并说明理由。
偏离项 D1 / D4 / D6 已改；D2 / D3 转为「已申报的范围外改动」保留（其中两处自造数字已回改）；D5 如实记录为「无法完全满足」。
分歧点 P1–P5 与待裁决 Q1–Q5 逐条处置，见下。

修完后的复验读数：
`check-doc-links.js docs` → 238 篇 / 0 问题；`punct-unify.js <目录>` → 0 处待改 / 0 落单 / 0 错配；
`an-compliance.js` → 9 / 9 断言 PASS，exit 0；构建 → exit 0，0 断锚点。

---

## 阻断项

### B1　引号字符集被整体换掉 —— **接受**

先自己复算了评审给的三组对照，全部复现：

| 语料 | `“ ”` | `「 」` |
| --- | --- | --- |
| 原文 `critical-thinking-playbook.md`（`d8f6d4c2`） | 213 | 3 |
| 对照成稿 `human-abilities-in-ai-era.md` | 137 | 6 |
| 同目录邻居 `criticalthinking.md` | 2 | 0 |
| **改前的新目录 13 篇** | 15 | **265** |

`PROJECT.md:165`（§7）写的是「中文句子（含表格、提示框）里的引号一律全角 `“ ”` `‘ ’`」，未列 `「」`。
方向上确实反了。评审说的「站内另有 165 篇用 `「` 共 1529 处」我也查了，成因是**两套用法**：
那 1529 处绝大多数是**栏目名与专名**（`PROJECT.md` 里的「春晓」「夜幕」「倚码千言」「改道」），
不是正文引用；本目录 265 处全部是正文引用，属 §7 管辖范围。

**已改**：264 处直角引号全部转为 `“ ”` `‘ ’`——`「」` 260 处（其中 3 处属加粗贴引号，见 B2）+ `『』` 4 处。
改后 `punct-unify.js <目录>` 报 **0 处待改 / 0 落单 / 0 错配**；`an-compliance.js` 断言 1 / 3 通过（全篇共 275 对全角双引号）。

**裁决 Q1 的处置：定 `“ ”`。** 理由是这不构成「改道」——§7 是现成口径，回改即可。
若反过来定 `「」`，才需要先走 §5 改 `PROJECT.md` §7 再回填。已按前者执行。

### B2　加粗包住引号 —— **接受**

`checks.md:133` 原文处是 `“**一半的真理**”`（合规），改后成了 `**「一半的真理」**`，字符集与嵌套方向同时出错。
评审指出的同类 `background.md:12` 也成立。

**已改 3 处**：
- `checks.md:133`：`**「一半的真理」**` → `“**一半的真理**”`
- `background.md:12`：`**「把东西做出来」不再是瓶颈。**` → `“**把东西做出来**”不再是瓶颈。`
- 同类第 3 处一并处理。

`an-compliance.js` 断言 2（`**“` / `”**` / `**‘` / `’**` 四种形态）现为 **0 处**。

### B3　复盘表少一个字段 —— **接受**

复算原文第 662 行：`| 信号类别（归并自 6.2 的八条） | 第 1 月 | 第 2 月 | 第 3 月 | 趋势 |` —— **5 列**，新版 4 列，
「趋势」列确已丢失，而正文仍在要求「看趋势，不看绝对值」。评审的定性（可填写模板拿走一个填写位 = 动了判据）成立。

**已改**：恢复「趋势」列。

按同一逻辑，我把评审列为**建议项**的 S4 也一并恢复：原文第 630 行的判断准确率表是 6 列
（`# | 当时的决定 | 把握程度 | 实际结果 | 判定（…） | 事后看，当时漏了什么`），新版把 `#` 与最后一列压成了表后一行文字。
既然是给读者填的表，字段就不该由我们合并。**已恢复为 6 列**，并删掉了那句补位文字。

**连带修正**：表恢复成 6 列后，「判定的关键在最后一列」这句话指错了列（判定是第 5 列），已改为「判定的关键在『判定』那一列」。
顺带说明：原文那句「最后一列」在 6 列表里其实也是错的，这次一并说准。

**代价**：最宽表从 4 列回到 6 列。见下 P2 与 `CHANGE.md` 的「口径修正二」。

### B4　验收 5/6/7 数字复算不出 + 验收 10 证据恒真 —— **接受（拆成两条）**

**（一）「13 个 ok」是恒真输出：成立。** 我自己读了 `check-style-one.js` 第 47 行，`console.log('ok')` 是无条件打印的，
它是完成信号，不含任何断言。用它做验收 10 的证据是虚的。

**已改**：新写 `.workbuddy/an-compliance.js`，**9 条断言**，任一失败即退出码 1：

```text
[PASS] 断言 1｜直角引号「」『』为 0（§7 中文引号用 “ ” ‘ ’）
[PASS] 断言 2｜加粗不与引号相邻（§7 写 “**重点**”，不写 **“重点”**）
[PASS] 断言 3｜全角双引号逐篇成对              共 275 对
[PASS] 断言 4｜punct-unify 普查：0 处待改 / 0 落单 / 0 错配
[PASS] 断言 5｜表格单元格内不加粗（§7）          0 处
[PASS] 断言 6｜正文无裸 < >（不变量 12：MDX 不炸）  0 处
[PASS] 断言 7｜内链不带结尾斜杠（不变量 11）       0 处
[PASS] 断言 8｜13 篇 front matter 五键齐全（不变量 5）
[PASS] 断言 9｜目录含 _category_.json 且 index.md 非空壳（不变量 3）
9 / 9 条断言通过
```

**（二）0.43 / 148 / 70 复算不出：成立，根因是立锚时没写口径。** 评审 T2 说得对，
`.workbuddy/` 下当时没有本轮使用的统计脚本可查。**这是我的流程错误**，不是数字造假——
当时是边诊断边写一次性脚本，脚本没留、口径没写，事后自然复算不出。

**已改**：新写 `.workbuddy/an-metrics.js`，把口径**写死在脚本头部注释里**（剔除范围、行分类规则、
句子的切分符、字数的算法、密度的分母），一条命令产出新旧并排读数：

| 指标 | 旧稿 | 新稿 | 验收要求 |
| --- | --- | --- | --- |
| 正文《书名》密度 | 5.17‰（135 处） | **0.40‰（10 处）** | ≤ 1‰ ✅ |
| 超 60 字句子 | 17 | **0** | 不高于原文 ✅ |
| 超 80 字句子 | 7 | **0** | — |
| 最长句 | 118 字 | **60 字** | — |
| 段落中位数 | 75 字 | **47 字** | ≤ 70 ✅ |
| 最长段 | 207 字 | **145 字** | 无超 200 字 ✅ |

**关于评审自己复算出的 0.28‰ 与「最长句 64」**：`《》` 计数我们其实**一致**（都是 10 处），
差在分母——评审把更多字符算进「正文」，我按脚本口径排除了表格、代码与篇末出处。
这类分歧不该靠争论解决，所以现在口径有唯一出处（`an-metrics.js` 头部），两边都能复跑。
**我接受「以脚本为准」这个机制，而不是坚持我那一版数字对。**

---

## 建议项

| # | 处置 | 说明 |
| --- | --- | --- |
| **S1** 篇末出处与正文不对应（9 处） | **接受，9 处全部处理** | 删 4 处（`decide` 框架效应、`guards` 观点形成的层次、`creativity-ai` 抬高下限、`checks` 省略信息）；改 4 处为与正文主张对齐（`checks`×2、`foundations`、`review`×3）；`review:128` 的「后见之明偏误」正文确有这个意思但没点名，改为照正文原话写。这条批评的要害是：**出处移到篇末是本次选的机制，机制本身就必须准**，对不上就是机制失效 |
| **S2** 季度复盘自造「20 分钟」 | **接受** | 原文该周期没有用时。改为「每季，一次」 |
| **S3** 「九成」无出处 | **接受** | §6.1 门禁要求「无法核实的数字宁可留白」。改为「大多数」 |
| **S4** 判断准确率表两列被并成文字 | **接受** | 与 B3 同因，已恢复 6 列（见 B3） |
| **S5** 正文仍留 10 处《书名》 | **部分接受，按验收 5 判合格** | DIAGNOSIS R1 写的「句子里的引用压到零」与验收 5 的「≤1 处/千字」自相矛盾，评审 P3 指出得对。**以验收 5 为准**，R1 那句话作废。留下的是主张本身需要具名的地方（决策四段式的来源、导览页的书单交代、逐问的归属），密度 0.40‰ |
| **S6** 「十二篇 / 十三篇」口径不一 | **接受** | `index.md` 描述的「十篇」已改「十二篇」；全文统一为「12 篇正文 + 1 篇导览 = 13 篇 md + 1 份 `_category_.json` = 14 项」 |
| **S7** 各篇新增块体例不一 | **记录不改** | 属裁决 3 明确放开的范围（人要求不强制统一体例骨架） |

---

## 偏离项

| # | 处置 |
| --- | --- |
| **D1** 删「趋势」列 | 即 B3，已改 |
| **D2** 书目口径未申报 | **保留并申报**。四处都不新增来源：《超越感觉》《思维突破》只是补全副标题；《创造性》与《创造力：心流与创新心理学》在原文 3.7 已自认是同一著作的两个中译（`appendix.md:22-24` 有专节）；《责任圈思维模型》是站内页面，原文写成书名号属笔误。已单列进 `CHANGE.md`「已申报的范围外改动」 |
| **D3** 新增原文没有的主张 | **保留并申报，其中自造数字已回改**。关键背景：人在裁决 ① 里明确授权「可以适当扩写拆分后的内容，以提高阅读体验和易理解性」——评审时这项授权未在仓库留痕，现已写入 `CHANGE.md` 抬头与待裁决表。其中两处属自造数据（「九成」「20 分钟」），与「扩写」无关，**已改回**（见 S2 / S3） |
| **D4** 收尾未完成 | **已完成**：`PROJECT.md` §10 第 8 条已改为现状、§7 已补「长文何时该拆章」一行、`DECISIONS.md` 已追加一行 |
| **D5** §9 三项授权无书面凭证 | **如实记录为无法完全满足**。授权发生在会话中，当时确实没落纸——这是我的疏漏。现已在 `CHANGE.md` 抬头写明授权来源与时点（2026-09-22 下午会话），作为仓库内唯一可查的痕迹 |
| **D6** 验收 1 数字错 | 已改：14 项（13 篇 md + 1 份 `_category_.json`） |

---

## 分歧点

- **P1（锚点代价表述）—— 接受，已更正。** 评审说「成立一半」，说得比我准。原文「站外深链仍然能打开正确的那一页」
  混淆了「页面打不开」与「内容不在这页了」；6 个退役 ID 指向的小节已整块搬走，落到导览页就是找不到。
  **已更正**，并按要求补上 5 个同名子页的新地址（`#background`/`#actions`/`#plan`/`#review`/`#pitfalls`
  → `.../critical-thinking-playbook/<同名>`）。卡片自身「所有小节级锚点一个不丢」而丢掉 6 个 H2 的自相矛盾也成立，
  口径已改为「**除 6 个章节级锚点外**，其余一个不丢」。
- **P2（R5 vs 验收 8）—— 按口径修正二处理。** 以「非目标第 3 条：不改任何一张表的判据」优先，
  两张复盘表恢复原文列数；验收 8 的口径改为「消除的是把整段说明塞进单元格的宽表；给读者填写的空表保留原文列数」。
  这不是放宽标准去迁就结果，而是承认原来的措辞把两类用途不同的表混为一谈了。
- **P3（R1 vs 验收 5）—— 按 S5 处理，以验收 5 为准。**
- **P4（`_category_.json` 的 `collapsed`）—— 保留 `true`。** 新目录合 §7；同级四份 `_category_.json` 均为 `false`
  是**站内既有偏离 §7 的现象**，不属本次范围。已在 `PROJECT.md` §10 记为待处理项，另开变更统一。
- **P5（12 篇 / 13 篇）—— 已统一口径**，见 S6。

---

## 待裁决

| # | 应答 |
| --- | --- |
| Q1 引号口径 | 定 `“ ”`。§7 是现成口径，回改即可，不需要走改道。已执行 |
| Q2 退役锚点的代价表述 | 接受，已按 P1 更正并补新地址 |
| Q3 验收 5/6/7 以哪套口径为准 | **以写出脚本者为准**：口径已写死在 `an-metrics.js` 头部，评审与产出方用同一份脚本，分歧自然消失 |
| Q4 「不改表格判据」是否涵盖删列 | 涵盖。已回填「趋势」列（B3），并连带恢复 S4 的两列 |
| Q5 收尾并入本次还是另开一轮 | 并入本次，已完成 |

---

## 未能验证的部分（评审 T1–T12 的回应）

| # | 回应 |
| --- | --- |
| **T1**「78 个偏误名」 | **该数字来源不明，我在本次全部材料里都找不到它**（`grep` 过 `DIAGNOSIS.md`，无此串），判为一次口误。本次清点以脚本实测为准：22 个偏误名 / 14 种谬误 / 18 条动作 / 136 处《书名》（去重 34 种）。该数字已不出现在任何材料中 |
| **T2** 验收 5/6/7 的口径与脚本 | 已补齐，见 B4（二）。脚本 `an-metrics.js` 可复跑 |
| **T3** 构建 exit 0 | 改动后已重跑。因 §6.2 要求先把 `build/` 改名，本次按同一做法执行；可提供重跑后的 `sitemap.xml` 与退出码 |
| **T4** 「首轮 4 处断锚点，修后 0 处」 | 当时没存构建日志，**无法提供凭证**。这一条我认账：现在能给的只是「修后复跑 exit 0」这个结果 |
| **T5** 验收 2 的结构体检脚本 | 原脚本没留。已由 `an-compliance.js` 断言 9 取代，可复跑 |
| **T6 / T7** 三处同源标注与 28 本书目事实 | **未查证，如实声明。** 我只核对到「新旧逐字一致」，原书原题 / ISBN / 目录都没查。这两项属内容侧的「待复核断言」，与本次拆章无关，沿用 `DIAGNOSIS` 里的「待复核」标记，不在本次范围内声称已核实 |
| **T8** §9 授权凭证 | 见 D5，如实记录 |
| **T9** 「48 处锚点链接」 | **实测不成立，已更正**：新目录内链接 **106 条**（带锚点 44 条）；站内其他文件指向本目录 **2 条**；直接指向 6 个退役 ID 的 **0 条**。`DIAGNOSIS.md` 5.3 的原数字保留原样，已加一行批注指向本单 |
| **T10** 站外深链失效面 | 接受为不可统计，已在卡片里明确写成「**已声明并接受的代价**」 |
| **T11** 与既有引用口径是否仍一致 | 与评审同：本次只做到链接级核对（`#pbl` / `#division` / `#ceiling` 命中），未做内容级逐句比对。**不声称已一致** |
| **T12** 语义变形的覆盖度 | 与评审同：抽样未发现变形，但**未逐句覆盖**，不声称全文无变形 |

---

## 仍未解决、留给下一轮的部分

1. **§9 授权没有落纸的凭证**（D5 / T8）——这是流程漏洞，不是内容问题。下一轮起，凡属请示项的授权，
   在会话中拍板后当场写进变更卡抬头，不再事后补记。
2. **同级四份 `_category_.json` 的 `collapsed: false` 偏离 §7**（P4）——已在 `PROJECT.md` §10 记为待处理，另开变更。
3. **书目事实与同源标注未查证**（T6 / T7）——需另开一轮去核原书信息。
4. **`build/` 的旧产物 `build_old_split`** 仍在本机，按守卫规则不自行删除。
