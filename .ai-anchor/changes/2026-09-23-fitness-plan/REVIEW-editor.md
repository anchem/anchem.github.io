# 评审单：`2026-09-23-fitness-plan`（可读性与可用性）

- **评审者**：独立评审者 · **资深文章编辑**（技术类与知识类长文方向；关注结构、可读性、读者能否照着做）
- **评审时间**：2026-09-23
- **信息隔离声明**：本次仅读取 `PROJECT.md`、`CHANGE.md`、`TEMPLATES/REVIEW.md`、`fitnessplan/` 12 篇正文、`fitnessplan/_category_.json`、被修改的两篇导览页（`health/index.md`、`health/sports/index.md`）与 `sports/` 下两篇既有页；**未读取创作过程的任何对话记录、未读取 `REPLY.md`、未查提交历史**。
  （说明：按要求保持隔离视角，不推测意图；一切判断只以产物文本与规范文件为据。）
- **本单不含任何修改方案**（依据硬约束 3）。「建议项」一栏只写缺口与后果，不写做法——做法属协作者与人的裁量范围。
- **不做重复机械校验**：直角引号、加粗与引号相邻、表格内加粗、裸尖括号、内链结尾斜杠、front matter 键位等脚本已覆盖项，除不变量核验表内逐条声明外不再单列。

---

## 结论

**`有条件通过`**

一句话理由：12 条不变量无一被破坏，分章颗粒度与「随时查今天练什么」的用法基本成立；但**同一件事在手册内部给出了互不相同的数字或顺序**（每周练几天、单手俯卧撑要多久、伪俄挺门槛是几次、同一动作几组几次、黄灯怎么降、日志字段在哪），共 10 处，读者无法自行裁决——全部处置完才放行。

---

## 阻断项（必须改，否则不放行）

| # | 位置（文件:行 / 章节） | 问题 | 依据 | 判据来源 |
| --- | --- | --- | --- | --- |
| 1 | `index.md:38`；`diagnosis.md:33`；`weekly-plan.md:24` 与 `weekly-plan.md:26-34`（二、标准周） | **「一周练几天、怎么分布」三处互不相同。**`index.md:38` 写「5 练 2 休：工作日三次加周末两次」；`diagnosis.md:33` 写「不是每天都练；周末在家两次」；而 `weekly-plan.md:26-34` 的表格实际排的是工作日**四**次（一、二、四、五）＋周六**一**次在家课＋周三「压心率跑或完全不练」＋周日休息。若把周三算作训练则是 6 练。读者按第一页的骨架去排下周课，会与第十章的表对不上 | 三处文本直接互斥；`diagnosis.md:33` 的 225 分钟也是按「五次 × 45 分钟」算出，因此分布口径直接决定时间预算是否成立 | 可读性焦点 2、8 |
| 2 | `index.md:19`；`index.md:51-52`；`index.md:58`；`weekly-plan.md:1`（sidebar_position 10）；`injury-prevention.md:9`（sidebar_position 9） | **三类顺序互不相同：导览宣言、sidebar 实际顺序、导览表「什么时候读」列。**`index.md:19` 宣称「先做取舍，再排周计划，最后才是动作」，但 sidebar 顺序是 取舍(2)→原则(3)→动作(4–8)→伤病预防(9)→周计划(10)；导览表又标伤病预防为「开始前必读」（`index.md:51`）、恢复为「每天」（`index.md:53`）。结果是「必读」章排在第 9 位，`injury-prevention.md:13` 自称「优先级高于本手册其他所有章节」，读者按 sidebar 顺读却要先读完 6 个动作章才看到它 | 手册自身的排序声明与文件顺序冲突；读者入场路径（sidebar）与推荐首次阅读路径（`index.md:58`：先读 2、3 章，直接跳到第 10 章排课）不一致 | 可读性焦点 1、5、7 |
| 3 | `upper-body.md:17`（一、表「现实预期」列）vs `roadmap.md:21-22` | **单手俯卧撑的时间预期差一倍。**`upper-body.md:17` 写「单手俯卧撑 …… 6 到 12 个月」，`roadmap.md:22` 把「单臂俯卧撑 1 到 3 次」放在 **24 个月**，且 `roadmap.md:21` 的 12 个月目标只到「离心单臂俯卧撑 3 组 3 次」。读者读到「6 到 12 个月」会以为一年内可解锁，按路线图却要两年 | 两处均是该目标的完成时点；`roadmap.md:15` 自称「参考坐标」，`upper-body.md:22` 又把这张表的最后一列称为「最重要的一列」 | 可读性焦点 8 |
| 4 | `diagnosis.md:81` vs `upper-body.md:49`、`upper-body.md:60`（`roadmap.md:77` 与后者一致） | **俄挺资格门槛的「伪俄挺俯卧撑」次数冲突：10 次 vs 8 次。**`diagnosis.md:81` 把「伪俄挺俯卧撑 10 次」列为「完成单手引体、人旗、俄式挺身」的翻译后能力，`upper-body.md:49` 的门槛写「伪俄挺俯卧撑 8 次，且前倾足够」、`:60` 阶梯第 2 级写「3 组 8 次」。读者无法判断该练到 8 还是 10 才允许进俄挺 | 这是**准入门槛**数字，不是训练量；`CHANGE.md` 风险缓解段明确要求高风险动作「必须在文内显式设门槛」 | 可读性焦点 8；与 `CHANGE.md`「风险缓解」段偏离 |
| 5 | `principles.md:39-45`（原则四）vs `lower-body.md:26-32`、`upper-body.md:26-33`、`upper-body.md:57-64`（俄挺阶梯）、`upper-body.md:72-80`（拉七级）、`upper-body.md:120-126`（人旗阶梯） | **原则四承诺「每个动作都要写清三级信息」（进级标准／退阶版本／常见错误，`principles.md:45` 断言「本手册每章的动作表都带这三样」），实际有两处没兑现：**① 拉七级、俄挺阶梯、人旗阶梯三张表**只有「进级标准」一列，没有「常见错误」列（对照 `lower-body.md:26-32`、`upper-body.md:26-33` 都带）；② **「进级标准」列口径不统一**——`lower-body.md:27` 写「每侧 20 次」、`:31` 写「每侧 3 组 5 次」；`upper-body.md:75-76` 写「10 次」「15 次」（单组最大次数？）；`upper-body.md:60` 又写「3 组 8 次」。读者照表判断「今天算不算合格」时，至少在拉、俄挺、人旗三条线上必须自己猜是单组最大次数还是多组 | 原则四自己规定「通常是能标准完成三组八次，或达到规定时间」，与只写数字的行不兼容；而这三条线恰是风险最高、最需要判断标准的三条 | 可读性焦点 2、8 |
| 6 | `diagnosis.md:83` vs `diagnosis.md:99-108`（六、基线自测）；`feet-grip.md:24`、`feet-grip.md:97-105`；`roadmap.md:19-22` | **基线自测清单与实际要测的项目对不上。**`diagnosis.md:101-108` 把自测固定为六项（悬垂、引体、单腿深蹲台阶、弓箭手俯卧撑、侧平板、单腿提踵）并称「六项测试放在同一天做完」，**但同一章 `diagnosis.md:83` 把「膝盖触墙距离达标」列为要达成的能力**；`feet-grip.md:24` 又称踝背屈（膝触墙）是「最该先测的一项」，`:97` 声明这些达标线「每 8 周自测时一起测」。`roadmap.md:19-22` 的「足踝与握力」列也只见悬垂一串数字。读者按 `index.md:58` 的「第一次」流程做基线，会漏掉决定单腿深蹲门槛的那一项（单腿闭眼站立同样漏项） | 「膝触墙」在手册内被反复称为前置条件（`lower-body.md:34`、`feet-grip.md:31`），却不在唯一的摸底清单里；两章各有一份「要测什么」，读者手上会有两份不重合的清单 | 可读性焦点 2、8 |
| 7 | `injury-prevention.md:39` vs `roadmap.md:51-53` | **同一黄灯信号给出两套不同的降级处方。**`injury-prevention.md:39`：「把该动作的强度与量各降一半，观察两周」；`roadmap.md:53`：出现黄灯信号时「**立即降一级**，并把该动作的量减半，持续两周」。一处降强度+降量，一处降台阶等级+降量。读者疼了之后按哪一份执行不明确；两处都不是可选项并列，而是同一触发条件的不同后果 | 「黄灯」是手册三张清单（§一）、路线图（§二）与速查（`roadmap.md:117`）共同的锚点，口径必须唯一 | 可读性焦点 2、8 |
| 8 | 毛巾悬垂：`upper-body.md:165`「3 组 30 秒」/ `feet-grip.md:79`「3 组 20 到 30 秒」/ `functional.md:108`「3 组 25 秒」。单手拎壶铃行走：`core.md:95`「30 到 40 米，每侧 3 组」/ `feet-grip.md:85`「走 30 米」/ `functional.md:107`「走 30 米，每侧 3 组」 | **同一动作用同一个名字出现在三篇，剂量却有三个值**（毛巾悬垂三个值、提箱走两个值）。同类的还有「吊环支撑」：`core.md:80`「20 到 30 秒，2 到 3 组」、`functional.md:75`「20 到 30 秒，3 组」、`functional.md:105`「3 组 25 秒」。读者当天的课表引用两篇时，值不同；不知道该执行哪一个 | 三处都不是交叉引用（无内链互指），而是各写一遍完整处方，属于「同一信息多源」而非「必要交叉引用」 | 可读性焦点 2、3、8 |
| 9 | `recovery.md:120` vs `principles.md:122-128`（六、训练日志最小字段） | **「训练日志的第五个字段」指代错误。**`recovery.md:120` 说三项自检「**训练日志的第五个字段就是给这个用的**，写下来」；但按 `principles.md:122-128`，五个字段依次是日期与睡眠时长、主项与成绩、主观强度、疼痛标记、一句话感受——第五个字段是「一句话感受」。三项自检里的**晨起心率**在五个字段里没有任何位置可记，睡眠质量也与「睡眠时长」不是同一字段。读者按原则六建好日志后，会发现这一章要求的记录无处落笔 | 两章共用「日志」这一个对象，字段表在 `principles.md`，用法说明在 `recovery.md`，指代必须能对上 | 可读性焦点 2、8 |
| 10 | `principles.md:70`（术语「离心」与记法「3-1-1」）；`lower-body.md:68`（术语「髋铰链」）；`principles.md:8`（关键词 RIR） | **关键术语与记法在首次出现处没有解释，且解释要么没有、要么落在更后面的章节且没与该术语挂钩：**① 「离心」在 `principles.md:70`（读者按导览第一个会读到的位置）作为处方出现——「离心三秒、底端停一秒，即 3-1-1」——全手册 18 处出现（逐篇 grep 计数：`lower-body.md:30,55,69,73,80,136,143`、`upper-body.md:32,80,99,101`、`diagnosis.md:80,105`、`injury-prevention.md:86,87`、`roadmap.md:21,71`、`principles.md:70`），**无一处说明「离心＝肌肉边被拉长边发力，也就是下放那一段」**；② 「髋铰链」正文首用于 `lower-body.md:68` 的动作表（当天就要照做），唯一的动作化解释在 `functional.md:51`，而那处并未出现「髋铰链」三个字（只写「髋往后推、背保持中立」）；③ **记法「3-1-1」三个数字只解释了前两个**（离心三秒、底端停一秒），第三个数字全程未说，而 `lower-body.md:46`、`lower-body.md:143` 直接要求「节奏 3-1-1」；④ `principles.md:8` 的 keywords 里出现了 `RIR`，正文从未出现该缩写，只用中文「留一到两次余力」（`principles.md:29-32`） | 读者画像明确「不是训练学或解剖学专业出身」、要求「语言通俗易懂」；这四条都是需要照着做的处方级信息，不是背景知识 | 可读性焦点 4 |

---

## 建议项（可以不改）

| # | 位置 | 建议（只描述缺口与后果，不含做法） | 不改的后果 |
| --- | --- | --- | --- |
| 1 | `core.md:23-28`（二、四类动作的分工，表） | 表头最后一列是「对应你的哪个目标」，前三行填的是目标（俄式挺身／单臂引体／人旗、单腿深蹲），第四行「主动屈曲」填的却是「悬垂举腿本身也是握力训练」——一列里混进了两种含义 | 按列读表的读者在这一格读不到目标，会以为表填错了 |
| 2 | `functional.md:92`（七、篮球） | 讲篮球的那一节里写「不是把**足球场**上的动作搬进练功房」，运动项目写错 | 读者会怀疑整节是套用别处文字，削弱该节可信度 |
| 3 | `functional.md:25` | 「它是本手册里**整含量**最高的动作」——「整含量」不是词 | 一句话需要读两遍才猜得出意思（应为「性价比」或「整合度」之类，但这是猜测，不是我应当填的答案） |
| 4 | `principles.md:130` | 「第二项是第一项的证据，第四项是安全阀」：第四项（疼痛标记）清楚，**第二项是第一项的证据**指代与逻辑都不明（成绩是睡眠时长的证据？） | 这是原则六给出的唯一一句「怎么用日志」的说明，读不懂等于没给 |
| 5 | `roadmap.md:17-22`（一、四条线的里程碑，6 列 5 行）；`core.md:23-28`（4 列长文本）；`recovery.md:66-72`（3 列长文本） | 里程碑表 6 列，单元格内含「分腿人旗 5 秒，团身俄挺 10 秒」这类长串，窄屏（手机碎片时间阅读是主要场景，见读者画像）需横向滚动才能对齐时间与项目 | 读者画像明确是「碎片时间读」，宽表在这类设备上最容易读串行；`PROJECT.md` §10 第 8 条也已把同类六列表记为待处理 |
| 6 | `principles.md:78-83`（三、一小时窗口）vs `weekly-plan.md:56-64`（三、六十分钟怎么切） | 框架表给「主项 25 到 30 分钟」，填好的样例里主项只占 12–30 分钟＝18 分钟（＋4 分钟热身组）；框架没有「缓冲、记录训练日志」一项，样例却有 56–60 分钟的缓冲 | 读者用框架估时间会与样例对不上，编排自己的课时不知道信哪一份 |
| 7 | `core.md:103`「悬垂举腿」vs `functional.md:77`「悬挂举腿」；`weekly-plan.md:33`「家整合课」vs `functional.md:99`「周末在家课」；`weekly-plan.md:42`「核心十分钟循环」vs `core.md:120`「十分钟核心循环」；`core.md:69`「单臂平板支撑」vs `core.md:26`「单臂平板」 | 同一动作或同一课程有三个名字对两个名字的写法 | 读者按名字在目录内检索、或对照两篇的处方时会以为它们是不同的东西 |
| 8 | `index.md:62`、`principles.md:113-116`、`injury-prevention.md:140-146`、`roadmap.md:79-81` | 关于弹力带的同一结论（「唯一建议添置」「唯一的伤病预防投入」「唯一一笔支出」）在四篇里各写一遍，措辞几乎相同 | 第四次的读者会开始跳读；且这句与「不花钱的预防手段」并置时口径不成立（见分歧点 1） |
| 9 | `core.md:76-81`、`functional.md:74-76` 与 `functional.md:105`（吊环支撑）；`core.md:93-99`、`feet-grip.md:84-88`、`functional.md:107`（单手拎壶铃行走）；`functional.md:58-68`、`weekly-plan.md:105-106`（爬行） | 同一个动作在三篇里各写一遍完整做法与剂量，彼此无内链；三处的值还不一致（见阻断项 8） | 读者不知道哪一篇是「正本」，改动其中一篇时另两篇会继续漂移 |
| 10 | `core.md:116` | 龙旗门槛写「平板支撑 2 分钟以上」，但本章的平板口径是 RKC 平板 20–30 秒（`core.md:56`），「普通平板支撑」在本手册内没有出现过剂量与判断标准 | 读者无法确认 2 分钟指哪种平板；按 RKC 的口径读会以为门槛不可达 |
| 11 | `diagnosis.md:107`（「抗侧屈，人旗的前置」）、`lower-body.md:97`（「比目鱼肌」）、`feet-grip.md:49`（「本体感觉」） | 术语首次出现处既无解释也无内链，而「抗侧屈」的定义在 `core.md:23-28`、「本体感觉」的解释在 `feet-grip.md:51`（同章下一段） | 同一类术语在别的篇里都做了链接或解释，唯独这几处没有，「该不该点进去看」变成读者的负担 |
| 12 | `functional.md:85-88`（转体跳／背包抛接／过顶砸地／负重旋转走）；`feet-grip.md:46-47`、`feet-grip.md:61`（脚跟行走、脚趾抬起、赤足训练） | 这些附属动作缺组数或次数（「背包抛接」连次数都没有），而原则四（`principles.md:39-45`）承诺每个动作都写清可判断的信息 | 读者读到这里只能自己编一个量，或者跳过不练——与「方案切实可行」的诉求直接冲突 |
| 13 | `weekly-plan.md:30`、`weekly-plan.md:48`、`functional.md:97`、`roadmap.md:85` | 「压心率跑」在四篇里作为周三的处方出现，但**没有给目标心率或区间**（`runningexercise.md` 有 E 区间 59–74% 的口径，手册内未指过去，`weekly-plan.md:30` 也未带内链） | 读者按「今天练什么、做到什么算合格」查表时，这一项没有「合格」的标准 |
| 14 | `index.md:68`（相关阅读「力量：俯卧撑 …… 动作要点与辅助训练」） | 目标页 `sports/pushup.md` 只有标题与空小节（无可读内容），描述高于实际；且该页用的是「第一式／第二式」编号体系，与 `upper-body.md:26-33` 的推台阶是两套体系，索引未说明关系 | 读者从新导览点过去会落在空页；将来 `pushup.md` 写实后，「跟哪一套练」会变成新的口径冲突（本次不动它已由人授权，见 `CHANGE.md` 非目标） |
| 15 | `recovery.md:80` | 「聚餐：把当天或第二天的训练安排成下肢课，正好用掉多出来的热量」——与本篇自己的主线（`recovery.md:45-49` 热量维持稳定、体脂由食堂决定）以及 `diagnosis.md:41`（不靠加量、只能更精准）并置时，读起来像「练一次可以抵一顿」 | 读者可能据此放宽饮食纪律，而饮食纪律正是 `diagnosis.md:43-49` 认定的三条瓶颈之一 |
| 16 | `diagnosis.md:112-116`（七）；`principles.md:118-130`（六）；`core.md:134-140`（八）；`recovery.md:112-120`（四）；`weekly-plan.md:100-109`（七）；`injury-prevention.md:140-146`（七） | **入口与出口不均衡。**这几篇结尾停在各自的最后一节，没有「下一步读哪篇」或回到导览的指针；而 `lower-body.md:148`、`upper-body.md:168`、`functional.md:111`、`roadmap.md:120` 都给了出口 | 读者画像要求「随时能查到今天练什么」，出口缺失时读者只能回浏览器后退或翻目录；六篇里包括两篇「必读」章（`principles.md`、`injury-prevention.md`），恰好是最需要给出下一步的两篇 |

> 说明：上表中「建议」一栏是**缺口描述**，不是做法。是否采纳、怎么改由协作者与人裁量（硬约束 3）。

---

## 偏离项

`CHANGE.md` 说要 A，实际做成了 B 的地方。

| # | CHANGE.md 的原文 | 实际做法 | 是否已在交付说明里声明 |
| --- | --- | --- | --- |
| 1 | 「收尾：更新 `PROJECT.md` §10：追加一行『`health/sports/fitnessplan/` 已建立，**13 篇**；两篇既有桩页 …… 仍未写实』」 | 截至本次评审，`PROJECT.md` §10 仍是原有 10 行，**无 fitnessplan 相关行**；另外「13 篇」与「12 篇 md ＋ 1 个 `_category_.json` ＝ 13 个文件」不是同一口径（篇数应为 12） | 否。**注意**：`PROJECT.md` 与 `DECISIONS.md` 的更新属第⑤步归集内容，若尚未到该步则属待办而非偏离——本行只记录事实与口径差，不作定性 |
| 2 | 「锚点（本次变更的过程记录）：`.ai-anchor/changes/2026-09-23-fitness-plan/{CHANGE,REVIEW-coach,REVIEW-rehab,REVIEW-editor,REPLY}.md`」 | 该目录实际有 `CHANGE.md`、`REVIEW-editor.md`（本次产出）与**一个未被声明的 `_probe.txt`**（内容为 `_category_.json` 副本＋高风险文件 mtime 探针） | 否。不影响站点（`.ai-anchor/` 不入构建，未触 §9 红线 3），但交付清单与实际内容不一致 |
| 3 | 「修改（2 个文件，**仅追加链接**，不动正文口径）」 | `sports/index.md:19` 是在实践记录列表末尾追加一行，与卡一致；`health/index.md:9` 却是在既有那行里插入链接并**改写了该行文字**（原文只列「运动 …… 四条」，现改为「跑步、力量训练的原则、方法与记录，以及一套长期徒手健身计划」） | 否。属措辞层面的改写，未触及任何被修改文件的正文口径，实际影响为零 |

**已按变更卡执行、无需列入偏离的两处（留证）**：待裁决 #2 的「不买也能练，代价是肩部养护选项变少」已在 `index.md:62`、`principles.md:116`、`injury-prevention.md:144` 写明；待裁决 #3 的「主线定为相对力量」及其理由已在 `diagnosis.md:62-70` 写出。待裁决 #1 的 `collapsed: true` 已在 `_category_.json:5` 落地。

---

## 不变量核验

**逐条对照，不许跳过任何一条。**（说明：脚本已覆盖项我只声明结论与复核方式，不重复报告细节。）

| # | 不变量 | 结论（已核验 / 被破坏 / 无法判断） | 证据 |
| --- | --- | --- | --- |
| 1 | 四个板块目录名不可改 | **已核验** | `docs/` 下仍为 `softwaremaster`／`selfdevelop`／`growthtree`／`lifeforfun`；本次全部产物落在 `docs/selfdevelop/health/sports/fitnessplan/` 与两篇既有 `index.md` 内。**边界**：我未跑 `git status/diff`，故无法断言工作区没有其它未声明的目录改动（见「未能验证的部分」） |
| 2 | 站点名 / URL / CNAME 不可改 | **已核验** | 12 篇正文与 2 篇导览页内无站点名、域名、`slug` 或自定义 URL 的写入（front matter 只有约定的五键，无 `slug`）；产物未触及 `CNAME` |
| 3 | 每个 docs 目录有 `_category_.json` ＋ 非空 `index.md` | **已核验** | 新目录 `fitnessplan/` 有 `_category_.json`（position 4／label「健身计划」／className `selfdevelop`），`index.md` 89 行，是导览页不是空壳（含「分章导览」表与「怎么用这份手册」）；其父链 `health/`、`health/sports/` 均有 `_category_.json` 与 `index.md`。**边界**：全站其它目录（约 22 个 `_category_.json`）的完整性不在本次范围，我只核了本次涉及的目录链 |
| 4 | 排序只用 `sidebar_position`，目录名与 label 无数字前缀 | **已核验** | 12 篇取值 1…12，唯一无重复（`index` 1 … `roadmap` 12）；目录名 `fitnessplan`、label「健身计划」均无数字前缀 |
| 5 | docs front matter 键位固定 | **已核验** | 逐篇读过：12 篇均为 `sidebar_position` / `title` / `description` / `tags` / `keywords`，无多余键、无缺键（与校验脚本口径一致，此处不重复报告） |
| 6 | 随笔 front matter 五键 ＋ 正文首行重复 H1 | **已核验（不适用）** | 本次变更产物清单内不含 `blog/` 任何文件；我未读 `blog/`，无从判断其它随笔现状——就本变更而言，该条无适用对象、未被触碰 |
| 7 | blog → docs 用绝对路径 `/docs/...` | **已核验（不适用）** | 同上：产物内无 blog 文件；12 篇手册内的站内链接全部是相对 `.md` 路径（符合 §7 对 docs 内互链的约定） |
| 8 | 色令牌只在 `custom.css` §1；深色选择器写法 | **已核验（不适用）** | 产物不含任何 `src/**` 文件；旁证：目录内 `_probe.txt` 的 mtime 探针显示 `src/css/custom.css`（09-17 20:34）、`docusaurus.config.js`（09-17 15:39）、`sidebars.js`（09-12）均早于本次变更日 09-23 |
| 9 | 天空层只切 `opacity` | **已核验（不适用）** | 同上；`src/theme/Root.js`（09-17 15:28）、`src/components/SkyBackdrop/index.js`（09-17 15:29）mtime 早于本次变更 |
| 10 | `build/`、`build_old*/`、`node_modules/`、`.docusaurus/` 不被手工编辑 | **已核验** | 产物清单内无产物目录文件；我未打开任何构建产物。**边界**：未跑 `git status`，无法证明工作区其它位置未被触碰 |
| 11 | `trailingSlash: false` 下所有内链不带结尾斜杠 | **已核验** | 逐篇把全部 `](…)` 过目：结尾斜杠 0 处；亦未见 `《x](y.md)` 这类非法形态（验收标准 #6 的人工过目项，我做了，0 处）。12 篇内的链接目标 `./*.md`、`../index.md`、`../runningexercise.md`、`../pushup.md` 均实际存在 |
| 12 | MDX 转义三条（裸 `<` `>`；表格内 `\|`；admonition 不插表格） | **已核验** | 逐篇通读未见正文裸 `<` `>`；表格单元格内的并列一律用全角「／」或顿号，未出现需要转义的半角竖线；全手册未使用 admonition，故第三条无适用对象。与脚本结论一致，此处不重复报告细节 |

---

## 分歧点

评审者与协作者不一致的地方。双方理由都要写全。（**限制**：评审时目录内没有 `REPLY.md`，协作者的理由我只能引 `CHANGE.md` 的「待裁决」栏与产物文本，无法引用其本人表述。）

| # | 分歧 | 评审者的理由 | 协作者的理由 |
| --- | --- | --- | --- |
| 1 | 「弹力带是这份计划里**唯一的伤病预防投入**」这句是否成立（`principles.md:116`、`injury-prevention.md:146`、`index.md:62`、`roadmap.md:81`） | 单看「投入」二字，它在手册内不唯一：`injury-prevention.md:100` 把「臀中肌与腘绳肌要单独练」「落地要有缓冲与控制」列为膝部预防手段、`:114-120` 又给了每日五分钟反久坐流程、`:78` 把「胸椎活动度练习」列为肩部预防——这些全部不花钱，且被明确写作「预防」。读者在 `injury-prevention.md` 同一章内会同时读到「唯一的预防投入」和若干不花钱的预防手段 | （据 `CHANGE.md` 待裁决 #2 倾向 A）引入弹力带是为了补上徒手给不到方向的肩袖阻力，「唯一」限定的是**器材口径**：在需要买的东西里只有它一根。且产物已按要求写明「不买也能练，代价是肩部养护选项变少」（`index.md:62`、`principles.md:116`、`injury-prevention.md:144`） |
| 2 | 阅读顺序以谁为准（阻断项 2） | 手册内部有三套顺序（`index.md:19` 的宣言、sidebar 的文件顺序、导览表「什么时候读」列），我把这视为结构缺陷：入场走 sidebar 的读者不会先读到被声明为「开始前必读」的 `injury-prevention.md`（第 9 位）与「优先级高于其他所有章节」的自我定位（`injury-prevention.md:13`） | （据产物推定的立场）sidebar 按「先总纲、后分项」排，具体阅读顺序交由 `index.md:56-60`「怎么用这份手册」给出；读者按导览走即可，不需要读序与置放序一致。且调整 `sidebar_position` 会牵动验收标准 #4「取值 1..12 无重复无空缺」的复算 |

---

## 待裁决

需要人来拍板的问题，附上选项与各自代价。

| # | 问题 | 选项 A（代价） | 选项 B（代价） |
| --- | --- | --- | --- |
| 1 | 每周到底是几练、怎么分布（阻断项 1） | 以 `weekly-plan.md:26-34` 的表为准（工作日四练＋周六在家一练＋周三可选跑＝5 至 6 练），改 `index.md:38` 与 `diagnosis.md:33` 的措辞 | 以 `index.md:38`／`diagnosis.md:33` 的「工作日三次＋周末两次＝5 练」为准，改标准周表格（需要挪掉或合并一次工作日课）。代价：走 A 要重算 `diagnosis.md:31-33` 的三小时四十五分钟／225 分钟预算；走 B 要牺牲一次工作日主项（推或拉其中之一），而拉正是手册认定的短板 |
| 2 | 单手俯卧撑的时间预期以哪个为准（阻断项 3） | 认可 `roadmap.md:22` 的 24 个月，改 `upper-body.md:17` 的「6 到 12 个月」 | 认可 6 到 12 个月，改 `roadmap.md:19-22` 的里程碑（三个月／六个月／十二个月三格都要前移）。代价：走 A 会让「相对力量主线」的近期回报看起来更远（可能打击执行意愿）；走 B 则等于把手册里最保守的那份时间表改激进，而 `roadmap.md:24` 那套「以年为单位」的口径正是风险缓解段要求保住的 |
| 3 | 台阶表「进级标准」的计数口径（阻断项 5） | 统一为「3 组 × 次数」（如 `upper-body.md:75-76` 的「10 次」「15 次」改为「3 组 10 次」「3 组 15 次」） | 统一为「单组最大次数」（如 `upper-body.md:60`「3 组 8 次」改为「8 次」），并在原则四里补一句口径说明。代价：走 A 要回改 4 张表的十余行，且与 `roadmap.md:19-22` 的里程碑数字（如「引体 15 次」）需要重新对齐含义；走 B 同样要回改，且「最大次数」在疲劳日不可测，与原则三「留一到两次余力」的日常口径可能互相干扰 |
| 4 | 阅读顺序：调 sidebar，还是改导览的说法（阻断项 2） | 调 `sidebar_position`，把 `injury-prevention.md` 提到动作章之前、`weekly-plan.md` 提到动作章之前，使文件顺序与 `index.md:19` 的宣言一致 | 保留现序，改 `index.md:19` 的宣言与导览表「什么时候读」列的措辞，明确「读序 ≠ 目录顺序」。代价：走 A 要重排 12 个 `sidebar_position`、并重跑验收标准 #4，同时 git 历史里的旧深链不通（本手册是新页，代价较小）；走 B 只动文字，但 `injury-prevention.md:13` 的「优先级高于其他所有章节」仍与它在 sidebar 上的第 9 位相冲突 |
| 5 | 基线自测是否纳入「膝触墙」与「单腿闭眼站立」（阻断项 6） | 纳入，`diagnosis.md:101-108` 由六项变八项，并重估「全程约 40 分钟」 | 不纳入，改 `feet-grip.md:24`「最该先测的一项」与 `:97`「每 8 周自测时一起测」的措辞，并在 `diagnosis.md:83` 说明膝触墙另测。代价：走 A 会拉长首次摸底时间，且 `diagnosis.md:99`「六项测试放在同一天做完」等三处数字要同步改；走 B 则让决定单腿深蹲门槛的那一项继续游离在两份清单之外 |
| 6 | `CHANGE.md` 待裁决栏的三项是否已获人确认 | 视为已确认（产物按倾向 A／A／A 落地：`_category_.json:5` 为 `true`、弹力带已引入并写了代价、主线为相对力量） | 视为「未经人确认的默认取值」，须在 `REPLY.md` 标注。代价：走 A 需要人的一句确认（`PROJECT.md` §10 第 10 条要求请示项当场落纸）；走 B 则该标注只能事后补，与 §10 第 10 条的整改方向相反 |

---

## 未能验证的部分

**明确声明哪些我没有检查过。** 这一栏最容易被省略，也最不该省。

- **未运行任何脚本与构建**：`an-compliance.js`（9 条文风与标点断言）、`check-doc-links.js`、`an-metrics.js`、`docusaurus build` 都没跑（本机 bash 不可用，PowerShell 输出未能回传）。因此**验收标准 #1、#2、#5、#6、#9 我只能人工复核，不为其「退出码 0／问题 0 处／12 个 URL 进 sitemap」背书**。第 6 条（非法链接形态）我按 `PROJECT.md` §6.2 的人工过目办法做了，12 篇内 0 处。
- **未读创作过程**：按信息隔离要求，我没读任何对话记录、提交历史、`REPLY.md`（评审时该文件不存在）。因此**协作者的立场、辩驳与已声明的取舍，我只能从 `CHANGE.md` 与产物反推**——分歧点一栏的「协作者理由」是二手引用，不是其本人表述。
- **未读另外两份评审单**：`REVIEW-coach.md` 与 `REVIEW-rehab.md` 在评审时尚未存在（目录内只有 `CHANGE.md` 与 `_probe.txt`）。训练学、解剖学、伤病风险的专业结论不在本单范围；**若那两份评审提出的改动与「可读性／可照做」冲突（例如为了严谨而加长句子、加限定条件），我没有参与权衡，也不能替其判断。**
- **未核对任何训练学事实**：负荷、恢复周期、门槛数字（8 次／10 次／12 次、体重四成等）在训练学上是否合理，我不判断；我只判断这些数字**在手册内部是否自洽**。凡我判为「冲突」处，都不代表其中某一个值在训练学上正确。
- **未查全站口径**：我只读了本手册 12 篇、两篇被修改的导览页、`sports/pushup.md` 与 `sports/runningexercise.md`（含 mtime 旁证）。`docs/` 其余两百余篇（含 `lifeforfun/basketball` 等可能与「篮球」「俯卧撑」「握力」重叠的页）**我没查**，所以「同一动作在全站的叫法是否统一」我只能对上述范围负责。
- **未测读者主观感受**：「术语解释够不够浅」「有没有句子要读两遍」我按逐句通读判断，**没有做可读性指数、句长分布统计、朗读测试或真实读者试用**。凡属我主观判断的（如「整含量」不可解、`principles.md:130` 指代不明），都已在条目里标明是可复核的文本事实，不含语气或措辞偏好。
- **未验证 `_probe.txt` 的来源**：不知它由谁、用什么生成，只记录它存在且未列入 `CHANGE.md` 的锚点清单；它同时是我判断「高风险文件未被改动」的旁证，因此这条旁证的强度取决于该文件本身的可靠性，我无法独立确认。
- **未做全量结构核验**：不变量 3 我只核了本次涉及的目录链（`fitnessplan/` → `sports/` → `health/`），未遍历全站 22 个 `_category_.json` 是否都有配套 `index.md`；`PROJECT.md` §10 第 2 条已记录的嵌套目录缺配置问题，与本次变更无关，未复查。
- **未跑 `git diff/status`**：因此「本次变更是否只有变更卡上声明的那 15 个文件被改动」我无法用版本控制证实，只能用产物通读与 `_probe.txt` 的 mtime 探针推断。

---

## 过闸检查（进入第⑤步前必须全部为是）

```text
[x] 结论明确（三选一）：有条件通过
[ ] 阻断项已有处置或已进入回应 —— 待 REPLY 逐条回应（10 条）
[x] 不变量清单 12 条全部标注了结论
[x] 分歧点已显性化，双方理由齐全（协作者理由为二手引用，已在栏内声明）
[x] 待裁决项已给选项与代价（6 项）
[x] 未能验证的部分已声明（9 条）
[x] 没有提出修改方案（建议项只写缺口与后果）
```
