# 评审单：2026-09-23-fitness-plan

- **评审者**：独立评审者 ｜ 角色：资深力量与体能教练（徒手训练与功能性训练方向，十年以上带练经验）
- **评审时间**：2026-09-23
- **信息隔离声明**：本次仅读取 `PROJECT.md`、`CHANGE.md` 与产物（`docs/selfdevelop/health/sports/fitnessplan/` 全 12 篇 + 被追加链接的 `sports/index.md`、`health/index.md`）；另仅读了目录树与 `sports/pushup.md`、`sports/runningexercise.md` 的既有正文用于口径比对。**未读取创作过程的任何对话记录，未读 git diff 与提交历史。** 本单只指出问题、给出依据与位置，不提出修改方案。

## 结论

`有条件通过`

一句话理由：手册的取舍意识、门槛意识与「以年为单位」的时间预期在方向上站得住，但内部存在 8 处自相矛盾或与自定规则直接冲突之处，其中「每周骨架与时间预算」「恢复间隔」「力竭口径」「进级判据」四项会直接决定读者照书执行时练什么、练多少，须先消除。

## 阻断项（必须改，否则不放行）

| # | 位置（文件:行 / 章节） | 问题 | 依据 | 判据来源 |
| --- | --- | --- | --- | --- |
| 1 | `index.md:38`；`weekly-plan.md:26-34`；`diagnosis.md:31-33` | 同一件「每周骨架」有三套互不相容的说法，且实际排课超出自设的时间预算。`index.md:38` 写「5 练 2 休：工作日三次加周末两次」；`weekly-plan.md:26-34` 实排为工作日四次（周一/二/四/五）＋周六一次＋周三一次跑步，合计 6 次课；`diagnosis.md:33` 则按「每周五次」算出有效 225 分钟并把 300 分钟当作总预算。按 `weekly-plan.md:26-34` 字面执行是 5×60＋30＝330 分钟，超出预算 10% | 三处对同一骨架的表述两两不等（3+2 / 4+1+周三 / 5 次）；`diagnosis.md:33`「按每周五次算，扣掉热身与收尾，有效训练时间约 225 分钟」 | 其余（时间预算未被诚实对待） |
| 2 | `weekly-plan.md:17`；`weekly-plan.md:32-33`；`functional.md:101-110` | 自定的恢复规则被自家排课违反。`weekly-plan.md:17` 规则一要求「同一肌群之间隔满 48 小时」，但周五为下肢课 B（`lower-body.md:139-146`：深蹲跳 4 组、保加利亚分腿蹲 4 组 10-12 次、落地控制每侧 3 组），周六紧接家整合课（`functional.md:104-108`：土耳其起立每侧 3 次×2 组、爬行 6 组 10 米、单臂提壶铃走每侧 3 组 30 米、毛巾悬垂 3 组）——间隔不足 24 小时，负荷落在同一批肌群与握力上。规则的字面只针对「下肢课」，对周六这条同负荷课程没有给出任何最小间隔判据 | `weekly-plan.md:17`「两次下肢课不要连着排」；`functional.md` 家课的负荷清单 | 其余（同一肌群恢复冲突） |
| 3 | `principles.md:29-35`；`lower-body.md:81`；`upper-body.md:88`；`upper-body.md:163` | 力竭口径三处互斥。`principles.md:31` 明令「默认不练到力竭……每组留一到两次余力」，`principles.md:35` 声明「只有一个例外：第 8 周的自测日」；但 `lower-body.md:81` 的维度手段之三直接要求「一组做到接近力竭」；`upper-body.md:163` 的拉日主项写「5 组 5 次」——在读者当前严格引体最大 5 次（`diagnosis.md:22`）的前提下，这等于组组力竭，且与该章自己的 `upper-body.md:88`「做 6 组 4 次，比硬做 3 组 5 次效果更好」自相矛盾 | 同一「是否允许力竭」在三章给出三种口径；拉日模板与同章方法一互相打架 | 其余（强度口径不可判定） |
| 4 | `principles.md:41`；`roadmap.md:32-38`；`lower-body.md:26-32`；`upper-body.md:27-33` | 「进级标准」有三种互斥定义，读者无法判定何时进级。`principles.md:41` 说「通常是能标准完成三组八次」；`roadmap.md:34` 说「能标准完成 3 组 5 次，且连续两次训练都做到」；而各章动作表用的是第三套：推的台阶是 12/10/5/3 次（`upper-body.md:27-33`），腿的六级台阶是 20/8/5/3/5/5 次（`lower-body.md:26-32`）。同一份手册的三处「进级」判据无法同时成立 | 三处原文并列；`roadmap.md:34` 的通用阈值与各章表内阈值无一相同 | 其余（进级标准不可判定） |
| 5 | `upper-body.md:15-20`；`roadmap.md:19-22` | 同一目标的时间预期相差一倍。`upper-body.md:17` 写「推：弓箭手俯卧撑每侧 8 次 → 单手俯卧撑，6 到 12 个月」；而 `roadmap.md:21-22` 把 12 个月定在「离心单臂俯卧撑 3 组 3 次」、把「单臂俯卧撑 1 到 3 次」放到 24 个月。按 roadmap，12 个月时离单手俯卧撑还差一整级，与「6 到 12 个月」不相容 | 两处对同一动作给出 6-12 个月与 24 个月；`roadmap.md:24` 又自认「参考坐标，不是承诺」，但并未声明与 `upper-body.md` 的差异以何者为准 | 其余（时间预期自相矛盾） |
| 6 | `upper-body.md:45-51`；`upper-body.md:111-116`；`diagnosis.md:81`；`diagnosis.md:60` | 人旗的门槛里有一项在手册中根本没有训练路径；同一门槛的数值又前后不一。`upper-body.md:116` 把人旗资格线之一定为「靠墙倒立保持 60 秒」，但全 12 篇没有任何倒立（含靠墙倒立）的台阶或练法，倒立只在 `injury-prevention.md:132` 的「腕痛时暂停」里被提到。另：人旗的引体门槛是 10 次（`upper-body.md:115`），而 `diagnosis.md:81` 把「单手引体、人旗、俄式挺身」共同的拉门槛写成「引体 15 次以上」；俄挺门槛 12 次（`upper-body.md:47`、`diagnosis.md:115`）又是第三个数值 | `upper-body.md:116` 的门槛项在全手册无对应练法；`diagnosis.md:81` 与 `upper-body.md:115` 对同一门槛给出 15 与 10 | 其余（门槛不可达 / 数值不一致） |
| 7 | `lower-body.md:66-71`；`lower-body.md:73`；`lower-body.md:136` | 北欧腘绳肌弯举的剂量与安全处理缺失，且与同课负荷叠加。它被写成「3 组 4 到 6 次离心」（`lower-body.md:69`）并作为下肢课 A 的常规辅助（`lower-body.md:136`），同一节课里还有「单腿硬拉每侧 3 组 8 次」（`lower-body.md:68`）——两项都是腘绳肌离心主导。该动作没有进级标准、没有退阶标准、没有频次上限、没有首次引入后的恢复间隔说明，仅在 `lower-body.md:73` 说「从双手撑地辅助开始」；且 `lower-body.md:73` 只给了「第一次做一定会觉得非常难」这一句定性描述。这与读者「伤痛风险很高宁愿慢慢来」的约束、与 `principles.md:45`「没有台阶的动作不算动作」的自我要求同时冲突 | 高离心负荷动作被安排在常规课次中，却未落入 `principles.md:39-45` 要求的三级信息（进级/退阶/常见错误） | 其余（动作台阶缺失 + 过量风险） |
| 8 | `upper-body.md:82-93`；`weekly-plan.md:26-34`；`upper-body.md:15-20` | 「拉的频次」承诺每周两次，排课只有一次。`upper-body.md:84` 明写引体专项「每周两次，别贪多」，`upper-body.md:18` 又把拉定为三大技巧的共同瓶颈（`diagnosis.md:25`、`diagnosis.md:91` 称其为最弱一环与关键路径），`roadmap.md:26` 更把拉的进度定为「整张表的关键路径」。但标准周里引体只出现在周四拉日（`weekly-plan.md:31`），周六家课只有吊环划船（`functional.md:102`）不含引体。承诺的频次与实排不符，关键路径实际每周只被触碰一次 | `upper-body.md:84` 的「每周两次」在 `weekly-plan.md:26-34` 与 `functional.md:101-110` 中找不到落点 | 其余（目标未排到位 / 与 CHANGE 意图冲突） |

> 说明：以上 8 条均**不是**不变量被破坏，而是方案本体与手册自定规则之间的冲突；按你给的判据优先级它们落在「其余」一档，但依「方案的专业性与可执行性」这一评审重点，均属必须先消除才能放行。

## 建议项（可以不改）

| # | 位置 | 建议 | 不改的后果 |
| --- | --- | --- | --- |
| 1 | `feet-grip.md:115`；`weekly-plan.md:44`；`weekly-plan.md:42`；`functional.md:108` | 「握力只在周四拉日做」这条规则与实排不符：`weekly-plan.md:42` 让周二核心循环含悬垂举腿（握力动作），`functional.md:108` 让周六家课含毛巾悬垂与单臂提壶铃走。需统一这张排程口径的表述 | 读者按哪一条执行都能被另一条指为违规；规则失去约束力 |
| 2 | `index.md:48`；`core.md:120-132`；`weekly-plan.md:56-64`；`upper-body.md:152-166`；`functional.md:101-110` | 「核心：每次课尾十分钟」这个定位没有进入任何一份 60 分钟模板：周一的 60 分钟切分（`weekly-plan.md:56-64`）里没有核心块，推日/拉日模板（`upper-body.md:152-166`）与家课模板（`functional.md:101-110`）同样没有。需明确它是否占用预算 | 承诺的「每次课都练核心」在时间上无落点，实际会变成「有空才练」 |
| 3 | `core.md:54-57`；`core.md:116` | 同一件事两套判据：`core.md:57` 说平板支撑「不是比时间，是比张力。30 秒高质量胜过两分钟瘫着」，`core.md:116` 又把龙旗门槛定为「平板支撑 2 分钟以上」 | 读者会被两个相反的标准夹住：门槛要求 2 分钟，正文告诉他时间不重要 |
| 4 | `core.md:4`；`core.md:23-28`；`index.md:48` | 该章 front matter 的 `description` 写「三类动作的分工」，正文表是四类（多出「主动屈曲」），`index.md:48` 的导览也只列三类 | 章节自我描述与正文不符，读者按 description 建立的心智模型会漏掉一整类 |
| 5 | `upper-body.md:77`；`diagnosis.md:60`；`roadmap.md:22`；`principles.md:106-116` | 「负重引体达体重四成」被写进门槛与两年里程碑，但器材表（`principles.md:111`）给的唯一负重工具是背包。按 70 公斤体重计约需 28 公斤，用背包实现既不可靠也有明显附加风险 | 里程碑与门槛落在一个用现有器材难以安全达成的条件上，可能诱使读者用不安全的方式凑重量 |
| 6 | `functional.md:90-97`；`weekly-plan.md:33`；`diagnosis.md:79` | 篮球表现被列为目标之一且在 `diagnosis.md:79` 有可判定表述（立定跳远、变向、落地控制改善），但周计划里篮球只以「或去打球」出现（`weekly-plan.md:33`），`functional.md:97` 又直接否定折返跑，全篇没有变向、减速、反复冲刺的任何量。另：`functional.md:92`「不是把足球场上的动作搬进练功房」在篮球语境下疑似项目写错 | 目标②中「提高篮球表现」这一半无法判定是否达成，也无从排期 |
| 7 | `principles.md:45`；`lower-body.md:66-71`、`lower-body.md:96-99`、`lower-body.md:109-114`；`upper-body.md:72-80`、`upper-body.md:120-126` | `principles.md:45` 声明「本手册每章的动作表都带这三样（进级标准/退阶版本/常见错误）」，但上列诸表实际只有组次或只有进级标准，没有退阶与常见错误 | 原则被自己的正文否定；读者对这些动作无法判断「做到了怎么进、卡住了怎么退」 |
| 8 | `lower-body.md:116`；`lower-body.md:139-145` | 「落地控制」被 `lower-body.md:120` 称为本手册最该认真做的动作，却在下肢课 B 里排在保加利亚分腿蹲 4 组 10-12 次之后（`lower-body.md:143-144`）；而 `lower-body.md:116` 与 `weekly-plan.md:19` 都主张需速度与神经质量的训练放在疲劳之前 | 减速能力的训练质量被主项疲劳稀释，且落地动作在疲劳下的风险高于其收益 |
| 9 | `injury-prevention.md:19-29`；全篇 | 手册有完整的红灯清单，但没有「开始前做一次基础的血压/心血管筛查」这一环。对 34 岁、即将开始高离心与高相对力量训练的人，这是长期主义口径下唯一缺的入口检查 | 唯一一道与「练到老」直接相关的入口筛查缺失，出事时的责任边界也更模糊 |
| 10 | `recovery.md:19-21`、`recovery.md:35-39`；`index.md:26`；`roadmap.md:19-22` | 读者现状是「实际睡眠六小时半到七小时半」，而 `<7 小时即当天降级、`<6 小时只做技术`（`recovery.md:37-38`）的阈值会高频触发。手册一边按 5 次课排周计划，一边给出一条会把相当比例的训练日自动降级的规则，却没有说明降级/忙周占比多大时 `roadmap.md:19-22` 的里程碑应顺延 | 里程碑按月给出却未挂接降级与忙周，读者会在正常执行的情况下被判为「卡住」 |

## 偏离项

`CHANGE.md` 说要 A，实际做成了 B 的地方。

| # | CHANGE.md 的原文 | 实际做法 | 是否已在交付说明里声明 |
| --- | --- | --- | --- |
| 1 | 无 | 逐条比对后未发现实质偏离：新增 13 个文件（12 篇 md + `_category_.json`）与 `CHANGE.md:23-37` 清单一致；`pushup.md`、`runningexercise.md` 的正文仍是原提纲、未提及本手册（符合 `CHANGE.md:51` 非目标）；待裁决 3 项均按 `CHANGE.md:103-106` 的「我的倾向」落地（`_category_.json` 为 `collapsed: true`、弹力带在 `index.md:62`／`principles.md:116`／`injury-prevention.md:142-146`／`roadmap.md:81` 四处一致声明、`diagnosis.md:64-70` 写明主线为相对力量） | — |
| 2 | `CHANGE.md:107`「须由人确认后才算成立；若人未表态，按『我的倾向』执行并在 `REPLY.md` 中标注为『未经人确认的默认取值』」 | 产物侧只能看到「按倾向执行」已发生；「人是否已确认」「REPLY.md 是否已标注」不在本次可读材料内 | 无法判断 |

## 不变量核验

**逐条对照，不许跳过任何一条。**

| # | 不变量 | 结论（已核验 / 被破坏 / 无法判断） | 证据 |
| --- | --- | --- | --- |
| 1 | 四个板块目录名不可改 | 已核验 | 当前树中 `docs/softwaremaster`、`docs/selfdevelop`、`docs/growthtree`、`docs/lifeforfun` 名称未变；本次只新增 `docs/selfdevelop/health/sports/fitnessplan/`（Glob 结果） |
| 2 | 站点名 / URL / CNAME 不可改 | 无法判断 | 未读 `CNAME`、`docusaurus.config.js`，也未做 diff；无证据可判「本次未触碰」 |
| 3 | 每个 docs 目录有 `_category_.json` + 非空 `index.md` | 已核验 | `fitnessplan/` 有 `_category_.json`（Read 全文）且 `index.md` 为完整导览（非空壳）；`sports/`、`health/`、`diet/` 三处 `_category_.json` 亦在树中（Glob 结果） |
| 4 | 排序只用 `sidebar_position`，无数字前缀 | 已核验 | 12 篇 `sidebar_position` 取值 1..12 连续且唯一（Grep 结果：1=index、2=diagnosis、3=principles、4=lower-body、5=upper-body、6=core、7=feet-grip、8=functional、9=injury-prevention、10=weekly-plan、11=recovery、12=roadmap）；目录名 `fitnessplan` 与 label「健身计划」无数字前缀 |
| 5 | docs front matter 键位 | 已核验 | 12 篇逐篇读过，`sidebar_position` / `title` / `description` / `tags` / `keywords` 五键齐全，键位与既有长文一致 |
| 6 | blog front matter 五键 + 首行重复 H1 | 无法判断 | 本次变更不涉及 blog，我也未检查 `blog/` 目录，无证据可判 |
| 7 | blog → docs 用绝对路径 | 无法判断 | 同上，未检查 `blog/` |
| 8 | 色令牌只在 `custom.css` §1，深色选择器写法 | 无法判断 | 未读 `src/css/custom.css`，也未做 diff |
| 9 | 天空层只切 opacity | 无法判断 | 未读 `src/theme/Root.js` 与 `src/components/**`，也未做 diff |
| 10 | 产物目录不被手工编辑 | 无法判断 | 未检查 `build/`、`build_old*/`、`node_modules/`、`.docusaurus/` 的当前状态与时间戳，也未做 diff |
| 11 | 内链不带结尾斜杠 | 已核验 | 12 篇内全部内链均为相对 `.md` 路径（`](./xxx.md)`、`](../index.md)` 等），无结尾斜杠、无绝对路径（Grep 全量结果；`index.md:66-68` 的 `../index.md`、`../runningexercise.md`、`../pushup.md` 形态合法） |
| 12 | MDX 转义三条 | 已核验 | 12 篇内未检出裸 `<` `>`（Grep `[<>]` 无匹配）；表格未使用未转义竖线；admonition 未出现在表格中间。**但**「构建不失败」只有跑构建才能证明，本次未跑（见「未能验证的部分」）——本条只到人工抽查这一层 |

## 分歧点

评审者与协作者不一致的地方。**双方理由都要写全**。

| # | 分歧 | 评审者的理由 | 协作者的理由 |
| --- | --- | --- | --- |
| 1 | 目标①（腿部维度）的资源投放 | 原始需求把「增加腿部肌肉维度」列为明确目标。实际周计划就是每周两次腿（`weekly-plan.md:28`、`weekly-plan.md:32`），且 `lower-body.md:143` 的保加利亚分腿蹲 4 组 10-12 次＋抱 12 公斤壶铃＋`lower-body.md:96` 的 8-15 次区间，正是标准的增肌范式。也就是说，方案一边在 `diagnosis.md:67` 声明「不采用『大重量低次数、每周两次腿』的增肌范式」，一边照该范式排课，且未说明为何「每周两次腿」在此不视为增肌范式。目标①因此在文字上被排在最后（`diagnosis.md:95`），在排课上却拿到了双课次与专门课名（下肢课 B「维度与爆发」），口径与实际不一致 | `diagnosis.md:64` 主线定为相对力量、腿围按自然增长；`diagnosis.md:68` 已声明代价「一年内腿围的变化可能只有两三厘米」；两套模板分设（`lower-body.md:19`）正是为了避免把力量、维度、爆发混在一节课里互相打折 |
| 2 | 每周时间预算是否被诚实对待 | `diagnosis.md:33` 认定有效 225 分钟「足以让中级训练者稳定进步」，但 `weekly-plan.md:26-34` 的实排是 6 次课（330 分钟），已超出该预算；`weekly-plan.md:60-62` 又给「单腿硬拉每侧 3 组 8 次」只留 10 分钟——按字面每侧 3 组即共 6 组，10 分钟内无法在合理休息下完成。预算是在「理想排法」下算出的，而不是按 `weekly-plan.md` 的真实排法 | `weekly-plan.md:36` 说明强度最高的两天排在最不易被加班碰掉的位置；`weekly-plan.md:68-98` 提供了最小可行版、减量周、15 分钟微训练三套降级路径，认为排法是弹性的 |
| 3 | 篮球表现的实现路径 | 篮球被列为目标之一（`diagnosis.md:79` 给了可判定表述），但 `functional.md:90-97` 用「直接打球比模拟更好」把它的训练含量降到几乎为零，`weekly-plan.md:33` 里篮球只是家课的一个「或」选项；减速、变向、反复冲刺这三项篮球体能的主要成分在手册里没有任何量。结论是「说得多、练得少」，目标②的后半无法验证 | `functional.md:94` 认为变向、急停、对抗、判断无法脱离赛场练出来；`functional.md:95` 把可迁移的基础（单腿力量、落地控制、臀外侧、后链减速）交给了下肢章，`functional.md:96` 也提示了疲劳打球的受伤风险 |

## 待裁决

需要人来拍板的问题，**附上选项与各自代价**。

| # | 问题 | 选项 A（代价） | 选项 B（代价） |
| --- | --- | --- | --- |
| 1 | 目标①（腿部维度）是否维持「排最后、自然增长」（`diagnosis.md:62-70`、`diagnosis.md:95`） | 维持：与相对力量主线一致（代价：需求中明确写下的一个目标长期可能不达标，且排课实际已在做维度课，文字与执行继续分叉） | 升为主线之一：与相对力量、单臂引体、俄挺正面冲突（代价：体重上升会按 `diagnosis.md:58-60` 的机制推迟技巧目标，须重写诊断与路线图两章） |
| 2 | 手册对外承诺的实际时间预算按哪一个口径 | A：按 `weekly-plan.md:26-34` 的真实排法（约 330 分钟/周）（代价：诊断自设的 225 分钟失效，恢复压力上移，`recovery.md:37-39` 的降级阈值会更频繁触发） | B：压回 225 分钟（代价：须删课次或压缩辅助，触及下肢/上肢/核心/功能性四章模板与周计划） |
| 3 | 人旗是否继续作为正式目标（`upper-body.md:19`、`upper-body.md:111-116`） | 保留：与原始需求「未来挑战人旗」一致（代价：门槛中的「靠墙倒立 60 秒」在手册内无练法，读者须自行补齐；引体门槛 10 与 `diagnosis.md:81` 的 15 也须择一） | 降为自选附加目标（代价：与需求承诺弱化，且 `roadmap.md:21-22` 的人旗里程碑需重排） |
| 4 | 「接近力竭」类手段是否允许存在（`lower-body.md:81` 的密集组 vs `principles.md:31` 的「默认不练到力竭」） | 允许并把它写成明确例外（代价：须重新定义全书强度口径，且 `principles.md:35` 的「唯一例外是自测日」被推翻） | 不采用（代价：`lower-body.md:77-82`「徒手练粗腿四个手段」少一个，维度线进一步变薄） |
| 5 | `CHANGE.md:101-107` 的三项待裁决是否已由人正式确认 | A：已确认（代价：无） | B：未确认，即三项均为「未经人确认的默认取值」（代价：按 `CHANGE.md:107` 须在 `REPLY.md` 显式标注，且弹力带、主线选择、`collapsed` 取值三件事都建立在默认值上） |

## 未能验证的部分

- **未运行任何脚本或构建。** `an-compliance.js`、`check-doc-links.js`、`check-style-one.js`、`an-metrics.js` 与 `docusaurus build`／`build/sitemap.xml` 一律未执行。因此 `CHANGE.md` 验收 1、2、3、5、6、9 的机器判据以及不变量 12 的「构建不失败」对我**不可判定**；我对不变量的核验只到「人工读取 12 篇全文＋目录树」这一层。
- **未读 git diff 与提交历史**（按信息隔离要求刻意不读）。因此所有「本次变更未触碰某文件」类的说法我都没有证据：不变量 2、8、9、10 与 6、7 我无法证明「未被改动」，只能说明「本次变更公告的范围不含它们」。
- **未读的文件**：`docusaurus.config.js`、`sidebars.js`、`package.json`、`src/**`、`static/**`、`CNAME`、`.ai-anchor/DECISIONS.md`、`.ai-anchor/changes/2026-09-23-fitness-plan/REPLY.md`（尚未生成）。
- **未做全站口径比对。** 除 `sports/index.md`、`health/index.md`、`sports/pushup.md`、`sports/runningexercise.md` 外，我未读任何既有文章。因此「与既有文章口径冲突」这一层只覆盖了这 4 篇；全站其余 230 余篇 docs 与本手册是否冲突，未查。
- **未做事实核查。** 手册中所有数字性论断（如「引体 5 次的人增重 5 公斤会掉到 3 次左右」`diagnosis.md:60`、「膝触墙脚趾距墙 10 厘米为达标」`feet-grip.md:26`、「握力是整体肌力水平的常用代理指标」`feet-grip.md:18`、「咖啡因半衰期约五到六小时」`recovery.md:27`）我均未查文献出处。我只能判断它们**内部是否自洽**，无法判断其**准确性**。
- **未评估动作演示质量。** 全篇无图无视频，我只能按文字判断台阶设计的逻辑，无法评估读者的实际动作质量、也无法评估文中所称的「动作要点」是否足够让人做对。
- **未测量任何个体数据。** 读者的真实体重、体脂、关节活动度、左右差异我都无从得知，因此 `diagnosis.md:19-23` 的能力评级与「单臂承重约为体重的四成到五成」这类换算，我无法核实。
- **未判断 `CHANGE.md` 三项待裁决是否已获人确认**（见待裁决 5）。
- **未判断本文与康复科、编辑两类评审的意见是否冲突**——另两份评审单不在我的可读材料内。

## 过闸检查（进入第⑤步前必须全部为是）

```text
[x] 结论明确（三选一）：有条件通过
[x] 阻断项已有处置或已进入回应：待 REPLY.md 逐条回应（8 条）
[x] 不变量清单 12 条全部标注了结论
[x] 分歧点已显性化，双方理由齐全
[x] 待裁决项已给选项与代价
[x] 未能验证的部分已声明
[x] 没有提出修改方案（本方只指出问题与位置）
```
