# 变更卡：把《批判性思维实战手册》从“压缩饼干”改写成普通人读得下去的手册

> 复制本文件到 `changes/YYYY-MM-DD-<slug>/CHANGE.md` 后填写。
> 一页为限。写不完说明这件事该拆成两次变更，而不是写长一点。

- **变更编号**：2026-09-22-playbook-readability-edit
- **日期**：2026-09-22
- **车道**：快车道 ——（判定依据：只改一个既有内容文件，不触碰不变量清单任何一条，可整文件回滚）

## 意图

在不改变任何论点、书目归属与操作步骤的前提下，把该文的阅读门槛降下来：
把三处表述不清的句子改写成含义确定的表达，把 3.4 里几条过简的“关键分寸”补上背景与推理，
并把全篇过度压缩的句子适当重述、扩写。**能被验证**：改完后由独立评审者按“逻辑与一致性 / 行文 / 阅读友好度”出评审单，阻断项清零。

## 范围

- `docs/selfdevelop/cognition/thinking/critical-thinking-playbook.md`（本次唯一被改的内容文件）

## 非目标

**本次明确不做的事，至少两条。**

- 不做：拆分章节、新建目录、改动 `sidebars.js`（单篇已近可读上限，但拆章要新建目录，属 §9 须请示人的动作，本轮不做）
- 不做：新增或删除任何论点、书目、操作步骤；不改 28 本书的书目归属与同书异名判定
- 不做：改动其它任何文档（含 `thinking/index.md`、`criticalthinking.md`，本轮不改索引）
- 不做：跑 `npm run build`（本机策略限制，见 `PROJECT.md` §6.2）

## 验收标准

怎么算做完了。**每一条都要可判定真假，能跑出结果的优先写成命令。**

| # | 验收项 | 怎么验（命令 / 动作） | 通过标准 |
| --- | --- | --- | --- |
| 1 | 引号口径零待改 | `node .workbuddy/punct-unify.js <文件>` | 需改动 0 个、落单引号无、错配无 |
| 2 | §7 加粗口径合规 | Grep `^\|.*\*\*` 与 `\*\*“|”\*\*` | 表格单元格内无加粗；无加粗紧贴引号 |
| 3 | 文风体检通过 | `node .workbuddy/check-style-one.js <文件>` | 输出 `ok` |
| 4 | 链接与锚点体检 | `node .workbuddy/check-doc-links.js docs` | 226 篇、0 问题 |
| 5 | 独立评审阻断项清零 | 读 `REVIEW.md` | 阻断项全部处置并在 `REPLY.md` 记录处置结果 |

> 写不出来的，最后一定演变成"我觉得还行"。写不出就先退回立锚，不要往下走。

## 影响面

逐条对照 `PROJECT.md` 第五节的「不变量清单」：

| 不变量 # | 本次是否触碰 | 说明 |
| --- | --- | --- |
| 1 | 否 | 目录名未动 |
| 2 | 否 | 站点名 / URL / CNAME 未动 |
| 3 | 否 | 未新增或移动目录，`_category_.json` 与 `index.md` 未动 |
| 4 | 否 | 未动排序 |
| 5 | 否 | front matter 键位不变（`sidebar_position` / `title` / `description` / `tags` / `keywords`） |
| 6 | 否 | 非随笔 |
| 7 | 否 | 非随笔 |
| 8 | 否 | 未动 CSS |
| 9 | 否 | 未动天空层 |
| 10 | 否 | 未动产物目录 |
| 11 | 否 | 文内相对链接未新增、未带结尾斜杠 |
| 12 | 否 | 已核：无裸 `<` `>`；表格单元格内无 `|`；admonition 不在表格内 |

结论：**不触碰任何不变量** → 可走快车道或标准道。

## 风险与回滚

- 最坏会怎样：扩写过程中不慎改变某个论点或书目归属的分寸，与已发布文章口径打架
- 怎么退回去：`git checkout -- docs/selfdevelop/cognition/thinking/critical-thinking-playbook.md`
- 回滚会不会丢内容：会丢掉本轮扩写与其前的用户微调，故 **回滚前必须确认 git 里已有本轮之前的版本**

## 待裁决

**需要人来拍板的选择题。给选项，也要给每个选项的代价。**

| # | 选项 | 代价 | 我的倾向 |
| --- | --- | --- | --- |
| 1 | A：维持单篇（800 行）/ B：拆成章节目录 | A：首屏继续偏长，靠 TOC 导航，两张宽表在窄屏横向滚动；B：新建目录属 §9 请示项，且全部 `{#custom-id}` 锚点需重挂，另需把 §7 五列表改成八个小节 | A（本轮），下一轮优先做 B |
| 2 | A：按用户口径继续扩写 / B：改为“扩写 + 拆章”一起做 | A：篇幅继续增长；B：一次变更承担两件事，回滚粒度变粗 | A（本轮） |

> 这一栏只有人能填。AI 可以起草，但必须由人确认后才算成立。

## 收尾

完成后要更新 `PROJECT.md` 的哪几节，以及是否需要在 `DECISIONS.md` 追加一行。

- `PROJECT.md` §10 第 8 条：补记本轮扩写后的实际行数
- `DECISIONS.md`：追加一行，记录本轮评审的隔离强度与结论

## 过闸检查（进入第②步前必须全部为是）

```text
[x] 验收标准可以被判定真假
[x] 非目标不是空的（至少两条）
[x] 影响面已逐条对照不变量清单
[x] 待裁决项给了选项与代价
[x] 车道已判定并写明
```
